---
layout: layouts/base.njk
title: "Federal Employee Benefits Workshop - April 9, 2026 | Capital Wealth"
description: "Exclusive federal benefits workshop for Hill AFB employees. Maximize FERS, TSP, FEGLI & healthcare benefits. Free workshop April 9 at Weber State University."
bodyClass: "federal-workshop-page"
---

<style>
/* Federal Workshop Page - Premium Capital Wealth Brand Styling */
.federal-workshop-page {
  background: var(--white);
}

/* Hero Section - Premium Federal Styling */
.federal-hero {
  background: linear-gradient(
    135deg, 
    rgba(25, 79, 144, 0.93) 0%, 
    rgba(20, 67, 122, 0.95) 100%
  ), 
  url('/assets/images/photos/CWA-Mountain-Couple-home-header.webp');
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
  min-height: 100vh;
  display: flex;
  align-items: center;
  color: var(--white);
  position: relative;
  overflow: hidden;
}

.federal-hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle at 30% 40%, rgba(255,255,255,0.08) 0%, transparent 50%);
  pointer-events: none;
}

.federal-hero-content {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-20);
  align-items: center;
  position: relative;
  z-index: 2;
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 var(--space-6);
}

@media (max-width: 1200px) {
  .federal-hero-content {
    grid-template-columns: 1fr;
    gap: var(--space-16);
    text-align: center;
    max-width: 800px;
  }
}

.federal-hero-left h1 {
  font-size: 4rem;
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: var(--space-6);
  color: var(--white);
  text-shadow: 0 2px 4px rgba(0,0,0,0.3);
}

.federal-hero-left .subtitle {
  font-size: 1.5rem;
  font-weight: 500;
  margin-bottom: var(--space-8);
  opacity: 0.95;
  color: var(--cw-orange);
  text-shadow: 0 1px 2px rgba(0,0,0,0.3);
}

.federal-hero-left .description {
  font-size: 1.2rem;
  font-weight: 400;
  margin-bottom: var(--space-10);
  opacity: 0.9;
  line-height: 1.6;
}

.event-highlights {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--space-6);
  margin-bottom: var(--space-8);
}

.highlight-item {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  text-align: center;
  border: 1px solid rgba(255, 255, 255, 0.2);
  transition: transform 0.3s ease;
}

.highlight-item:hover {
  transform: translateY(-2px);
}

.highlight-icon {
  font-size: 2.5rem;
  margin-bottom: var(--space-3);
  display: block;
}

.highlight-text {
  font-weight: 600;
  font-size: 1.1rem;
}

.event-details-badge {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(20px);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  border: 1px solid rgba(255, 255, 255, 0.2);
  box-shadow: 0 8px 32px rgba(0,0,0,0.1);
}

.event-details-badge h3 {
  color: var(--cw-orange);
  font-size: 1.4rem;
  font-weight: 700;
  margin-bottom: var(--space-6);
  text-align: center;
}

.event-detail {
  display: flex;
  align-items: center;
  margin-bottom: var(--space-4);
  font-size: 1.1rem;
  font-weight: 600;
  padding: var(--space-3);
  border-radius: var(--radius);
  background: rgba(255, 255, 255, 0.05);
}

.event-detail:last-child {
  margin-bottom: 0;
}

.event-icon {
  font-size: 1.4rem;
  margin-right: var(--space-4);
  color: var(--cw-orange);
  min-width: 24px;
}

/* Registration Form - Premium Design */
.registration-card {
  background: var(--white);
  border-radius: var(--radius-xl);
  padding: var(--space-12);
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
  position: relative;
  overflow: hidden;
  max-width: 500px;
  margin: 0 auto;
}

.registration-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6px;
  background: linear-gradient(90deg, var(--cw-primary), var(--cw-orange));
}

.registration-card h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: var(--cw-primary);
  margin-bottom: var(--space-4);
  text-align: center;
  line-height: 1.2;
}

.registration-subtitle {
  text-align: center;
  color: var(--gray-600);
  font-size: 1.2rem;
  margin-bottom: var(--space-4);
  font-weight: 500;
  line-height: 1.5;
}

.urgency-banner {
  background: linear-gradient(135deg, #DC2626, #EF4444);
  color: white;
  padding: var(--space-4);
  border-radius: var(--radius-lg);
  text-align: center;
  margin-bottom: var(--space-8);
  font-weight: 700;
  font-size: 1.1rem;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.8; }
}

.form-group {
  margin-bottom: var(--space-6);
}

.form-group label {
  display: block;
  font-weight: 700;
  color: var(--cw-primary);
  margin-bottom: var(--space-3);
  font-size: 1.1rem;
}

.form-group input,
.form-group select {
  width: 100%;
  padding: var(--space-4);
  border: 2px solid var(--gray-200);
  border-radius: var(--radius-lg);
  font-size: 1.1rem;
  font-family: var(--font-family);
  transition: all 0.3s ease;
  background: var(--white);
  font-weight: 500;
}

.form-group input:focus,
.form-group select:focus {
  outline: none;
  border-color: var(--cw-primary);
  box-shadow: 0 0 0 4px rgba(25, 79, 144, 0.1);
  transform: translateY(-1px);
}

.federal-submit-btn {
  width: 100%;
  background: linear-gradient(135deg, var(--cw-primary), var(--cw-secondary));
  color: var(--white);
  padding: var(--space-5) var(--space-8);
  border: none;
  border-radius: var(--radius-lg);
  font-size: 1.3rem;
  font-weight: 800;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 8px 25px rgba(25, 79, 144, 0.3);
  margin-top: var(--space-8);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.federal-submit-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 15px 35px rgba(25, 79, 144, 0.4);
}

.form-footer {
  text-align: center;
  margin-top: var(--space-8);
  padding-top: var(--space-6);
  border-top: 2px solid var(--gray-200);
}

.contact-info {
  color: var(--gray-600);
  font-size: 1rem;
  font-weight: 600;
}

.contact-info strong {
  color: var(--cw-primary);
  font-size: 1.1rem;
}

/* Value Proposition Section */
.value-section {
  padding: var(--space-24) 0;
  background: linear-gradient(135deg, var(--gray-50), var(--white));
}

.value-header {
  text-align: center;
  max-width: 900px;
  margin: 0 auto var(--space-20);
}

.value-header h2 {
  font-size: 3rem;
  color: var(--cw-primary);
  margin-bottom: var(--space-6);
  font-weight: 800;
  line-height: 1.2;
}

.value-header .description {
  font-size: 1.3rem;
  color: var(--gray-600);
  font-weight: 500;
  line-height: 1.6;
}

.value-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: var(--space-8);
  margin-top: var(--space-16);
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
}

.value-card {
  background: var(--white);
  border-radius: var(--radius-xl);
  padding: var(--space-10);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  text-align: left;
  border: 1px solid var(--gray-100);
}

.value-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
}

.value-icon {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, var(--cw-primary), var(--cw-orange));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--space-6);
  font-size: 2.2rem;
  color: var(--white);
  box-shadow: 0 8px 20px rgba(25, 79, 144, 0.3);
}

.value-card h3 {
  font-size: 1.6rem;
  color: var(--cw-primary);
  margin-bottom: var(--space-4);
  font-weight: 700;
  line-height: 1.3;
}

.value-card p {
  color: var(--gray-600);
  line-height: 1.7;
  font-size: 1.1rem;
  font-weight: 500;
}

/* Host Section */
.host-section {
  padding: var(--space-24) 0;
  background: var(--white);
}

.host-content {
  display: grid;
  grid-template-columns: 350px 1fr;
  gap: var(--space-16);
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

@media (max-width: 900px) {
  .host-content {
    grid-template-columns: 1fr;
    text-align: center;
    gap: var(--space-12);
  }
}

.host-image {
  border-radius: var(--radius-xl);
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  position: relative;
}

.host-image img {
  width: 100%;
  height: auto;
  display: block;
}

.host-label {
  color: var(--cw-orange);
  font-weight: 800;
  font-size: 1rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: var(--space-4);
}

.host-details h3 {
  font-size: 2.5rem;
  color: var(--cw-primary);
  margin-bottom: var(--space-3);
  font-weight: 800;
  line-height: 1.2;
}

.host-title {
  color: var(--gray-600);
  font-size: 1.3rem;
  font-weight: 600;
  margin-bottom: var(--space-8);
}

.credentials {
  margin-bottom: var(--space-8);
}

.credential-item {
  display: flex;
  align-items: flex-start;
  margin-bottom: var(--space-4);
  color: var(--gray-700);
  font-weight: 600;
  font-size: 1.1rem;
}

.credential-check {
  color: var(--cw-orange);
  margin-right: var(--space-4);
  font-weight: 800;
  font-size: 1.2rem;
  margin-top: 2px;
  min-width: 20px;
}

.host-quote {
  background: linear-gradient(135deg, var(--gray-50), var(--white));
  padding: var(--space-8);
  border-radius: var(--radius-xl);
  border-left: 6px solid var(--cw-orange);
  font-style: italic;
  color: var(--gray-700);
  line-height: 1.7;
  font-size: 1.1rem;
  font-weight: 500;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
}

/* Social Proof Section */
.social-proof-section {
  padding: var(--space-20) 0;
  background: linear-gradient(135deg, var(--cw-primary), var(--cw-secondary));
  color: var(--white);
}

.social-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--space-8);
  text-align: center;
  max-width: 1000px;
  margin: 0 auto;
}

.stat-item {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.stat-number {
  font-size: 3rem;
  font-weight: 800;
  color: var(--cw-orange);
  display: block;
  margin-bottom: var(--space-2);
}

.stat-label {
  font-size: 1.1rem;
  font-weight: 600;
  opacity: 0.9;
}

/* Urgency CTA Section */
.urgency-cta {
  background: linear-gradient(135deg, var(--gray-900), var(--gray-800));
  padding: var(--space-20) 0;
  color: var(--white);
  text-align: center;
  position: relative;
  overflow: hidden;
}

.urgency-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url('/assets/images/photos/CWA-Mountain-Couple-home-header.webp');
  background-size: cover;
  background-position: center;
  opacity: 0.1;
}

.urgency-content {
  max-width: 800px;
  margin: 0 auto;
  position: relative;
  z-index: 2;
}

.urgency-cta h2 {
  font-size: 3rem;
  color: var(--white);
  margin-bottom: var(--space-6);
  font-weight: 800;
  line-height: 1.2;
}

.urgency-text {
  font-size: 1.4rem;
  margin-bottom: var(--space-10);
  opacity: 0.95;
  font-weight: 500;
  line-height: 1.6;
}

.urgency-details {
  background: rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-xl);
  padding: var(--space-8);
  margin-bottom: var(--space-10);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.urgency-details h3 {
  color: var(--cw-orange);
  font-size: 1.6rem;
  margin-bottom: var(--space-6);
  font-weight: 700;
}

.event-summary {
  font-size: 1.2rem;
  font-weight: 600;
  line-height: 1.6;
}

.cta-buttons {
  display: flex;
  gap: var(--space-6);
  justify-content: center;
  flex-wrap: wrap;
}

.cta-primary {
  background: linear-gradient(135deg, var(--cw-orange), #E07A3A);
  color: var(--white);
  padding: var(--space-5) var(--space-10);
  border-radius: var(--radius-lg);
  font-weight: 800;
  text-decoration: none;
  transition: all 0.3s ease;
  box-shadow: 0 8px 25px rgba(240, 159, 84, 0.4);
  font-size: 1.3rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.cta-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 15px 40px rgba(240, 159, 84, 0.6);
}

.cta-secondary {
  color: var(--white);
  text-decoration: none;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-5);
  font-size: 1.1rem;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: var(--radius-lg);
  transition: all 0.3s ease;
}

.cta-secondary:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: rgba(255, 255, 255, 0.5);
}

/* Mobile Optimization */
@media (max-width: 768px) {
  .federal-hero {
    background-attachment: scroll;
    min-height: auto;
    padding: var(--space-20) 0;
  }
  
  .federal-hero-left h1 {
    font-size: 2.5rem;
    line-height: 1.2;
  }
  
  .federal-hero-left .subtitle {
    font-size: 1.3rem;
  }
  
  .federal-hero-left .description {
    font-size: 1.1rem;
  }
  
  .registration-card {
    padding: var(--space-8);
    margin: 0 var(--space-4);
  }
  
  .registration-card h2 {
    font-size: 2rem;
  }
  
  .cta-buttons {
    flex-direction: column;
    align-items: center;
  }
  
  .value-grid {
    grid-template-columns: 1fr;
  }
  
  .value-header h2 {
    font-size: 2.2rem;
  }
  
  .urgency-cta h2 {
    font-size: 2.2rem;
  }
  
  .event-highlights {
    grid-template-columns: 1fr;
  }
  
  .social-stats {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 480px) {
  .federal-hero-left h1 {
    font-size: 2rem;
  }
  
  .registration-card {
    margin: 0 var(--space-2);
    padding: var(--space-6);
  }
  
  .value-card {
    padding: var(--space-6);
  }
  
  .social-stats {
    grid-template-columns: 1fr;
  }
}

/* Loading State */
.federal-submit-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
  transform: none;
}

/* Success State */
.form-success {
  background: linear-gradient(135deg, #059669, #10B981);
  color: white;
  padding: var(--space-6);
  border-radius: var(--radius-lg);
  text-align: center;
  font-weight: 700;
  font-size: 1.2rem;
}
</style>

<script>
// Enhanced Federal Workshop Conversion Tracking
function trackFederalWorkshopRegistration() {
  console.log('Federal workshop registration tracked');
  
  // Facebook Pixel tracking
  if (typeof fbq !== 'undefined') {
    fbq('track', 'CompleteRegistration', {
      content_name: 'Federal Benefits Workshop April 9 2026',
      content_category: 'Workshop Registration',
      content_ids: ['federal-workshop-april-9-2026'],
      value: 750,
      currency: 'USD',
      predicted_ltv: 5000,
      custom_data: {
        workshop_date: '2026-04-09',
        location: 'Weber State University Davis Campus',
        target_audience: 'Hill AFB Federal Employees'
      }
    });
  }
  
  // Google Analytics tracking
  if (typeof gtag !== 'undefined') {
    gtag('event', 'federal_workshop_registration', {
      event_category: 'workshop',
      event_label: 'April 9 2026 Hill AFB',
      value: 750,
      workshop_date: '2026-04-09'
    });
  }
}

// Enhanced lead scoring for federal employees
function trackFederalBenefitsInterest() {
  if (typeof fbq !== 'undefined') {
    fbq('track', 'Lead', {
      content_name: 'Federal Benefits Workshop Interest',
      content_category: 'Federal Employee Lead',
      source: 'Landing Page Engagement',
      custom_data: {
        lead_quality: 'High - Federal Employee',
        interest_level: 'Workshop Attendance'
      }
    });
  }
}

// Track engagement at different scroll depths
let engagementTracked = {
  quarter: false,
  half: false,
  threequarter: false
};

window.addEventListener('scroll', function() {
  const scrollPercent = (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;
  
  if (!engagementTracked.quarter && scrollPercent > 25) {
    engagementTracked.quarter = true;
    if (typeof gtag !== 'undefined') {
      gtag('event', 'scroll_depth', { event_category: 'engagement', value: 25 });
    }
  }
  
  if (!engagementTracked.half && scrollPercent > 50) {
    engagementTracked.half = true;
    trackFederalBenefitsInterest();
  }
  
  if (!engagementTracked.threequarter && scrollPercent > 75) {
    engagementTracked.threequarter = true;
    if (typeof gtag !== 'undefined') {
      gtag('event', 'scroll_depth', { event_category: 'engagement', value: 75 });
    }
  }
});

// Form validation and enhancement
document.addEventListener('DOMContentLoaded', function() {
  const form = document.querySelector('#federal-workshop-form');
  
  if (form) {
    // Add real-time validation
    const inputs = form.querySelectorAll('input, select');
    inputs.forEach(input => {
      input.addEventListener('blur', function() {
        validateField(this);
      });
      
      input.addEventListener('input', function() {
        if (this.classList.contains('error')) {
          validateField(this);
        }
      });
    });
    
    form.addEventListener('submit', function(e) {
      e.preventDefault();
      
      // Validate all fields
      let isValid = true;
      const requiredFields = form.querySelectorAll('[required]');
      
      requiredFields.forEach(field => {
        if (!validateField(field)) {
          isValid = false;
        }
      });
      
      if (isValid) {
        trackFederalWorkshopRegistration();
        
        const submitBtn = form.querySelector('.federal-submit-btn');
        const originalText = submitBtn.textContent;
        
        submitBtn.textContent = 'Securing Your Spot...';
        submitBtn.disabled = true;
        
        // Simulate API call
        setTimeout(() => {
          submitBtn.textContent = 'Registration Confirmed!';
          setTimeout(() => {
            window.location.href = '/federal-workshop-confirmed/';
          }, 1000);
        }, 2000);
      }
    });
  }
  
  function validateField(field) {
    const value = field.value.trim();
    let isValid = true;
    let errorMessage = '';
    
    // Remove existing error styling
    field.classList.remove('error');
    const existingError = field.parentNode.querySelector('.error-message');
    if (existingError) {
      existingError.remove();
    }
    
    // Check if required field is empty
    if (field.hasAttribute('required') && !value) {
      isValid = false;
      errorMessage = 'This field is required';
    }
    
    // Email validation
    if (field.type === 'email' && value) {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(value)) {
        isValid = false;
        errorMessage = 'Please enter a valid email address';
      }
    }
    
    // Phone validation
    if (field.type === 'tel' && value) {
      const phoneRegex = /^\(?[\d\s\-\(\)\+\.]{10,}$/;
      if (!phoneRegex.test(value)) {
        isValid = false;
        errorMessage = 'Please enter a valid phone number';
      }
    }
    
    if (!isValid) {
      field.classList.add('error');
      field.style.borderColor = '#DC2626';
      
      const errorElement = document.createElement('div');
      errorElement.className = 'error-message';
      errorElement.style.color = '#DC2626';
      errorElement.style.fontSize = '0.875rem';
      errorElement.style.marginTop = '0.5rem';
      errorElement.style.fontWeight = '600';
      errorElement.textContent = errorMessage;
      
      field.parentNode.appendChild(errorElement);
    } else {
      field.style.borderColor = '#10B981';
    }
    
    return isValid;
  }
});

// Smooth scroll for anchor links
document.addEventListener('DOMContentLoaded', function() {
  const links = document.querySelectorAll('a[href^="#"]');
  
  links.forEach(link => {
    link.addEventListener('click', function(e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
});
</script>

{# Federal Benefits Workshop Landing Page #}
<section class="federal-hero">
  <div class="federal-hero-content">
    <div class="federal-hero-left">
      <h1>Maximize Your Federal Benefits</h1>
      <p class="subtitle">Exclusive Workshop for Hill AFB Employees</p>
      <p class="description">Join fellow federal employees for this comprehensive benefits workshop designed specifically for Air Force personnel at Hill AFB.</p>
      
      <div class="event-highlights">
        <div class="highlight-item">
          <span class="highlight-icon">💰</span>
          <div class="highlight-text">Maximize TSP</div>
        </div>
        <div class="highlight-item">
          <span class="highlight-icon">🛡️</span>
          <div class="highlight-text">Optimize FERS</div>
        </div>
        <div class="highlight-item">
          <span class="highlight-icon">📈</span>
          <div class="highlight-text">Tax Strategies</div>
        </div>
      </div>
      
      <div class="event-details-badge">
        <h3>📅 Workshop Details</h3>
        <div class="event-detail">
          <span class="event-icon">📅</span>
          <span><strong>Thursday, April 9, 2026</strong></span>
        </div>
        <div class="event-detail">
          <span class="event-icon">⏰</span>
          <span><strong>9:00 AM - 11:00 AM</strong></span>
        </div>
        <div class="event-detail">
          <span class="event-icon">📍</span>
          <span><strong>Weber State University - Davis Campus</strong></span>
        </div>
        <div class="event-detail">
          <span class="event-icon">🎟️</span>
          <span><strong>FREE</strong> (Limited Seating)</span>
        </div>
        <div class="event-detail">
          <span class="event-icon">🎯</span>
          <span><strong>Hill AFB Federal Employees Only</strong></span>
        </div>
      </div>
    </div>
    
    <div class="registration-card">
      <h2>Reserve Your Seat</h2>
      <p class="registration-subtitle">Join fellow federal employees for this exclusive benefits optimization workshop</p>
      
      <div class="urgency-banner">
        🔥 Only 12 Spots Remaining - Register Now!
      </div>
      
      <form id="federal-workshop-form" action="/api/submit-lead" method="post">
        <div class="form-group">
          <label for="firstName">First Name *</label>
          <input type="text" id="firstName" name="firstName" required placeholder="Your first name">
        </div>
        
        <div class="form-group">
          <label for="lastName">Last Name *</label>
          <input type="text" id="lastName" name="lastName" required placeholder="Your last name">
        </div>
        
        <div class="form-group">
          <label for="email">Email Address *</label>
          <input type="email" id="email" name="email" required placeholder="your.email@domain.com">
        </div>
        
        <div class="form-group">
          <label for="phone">Phone Number *</label>
          <input type="tel" id="phone" name="phone" required placeholder="(555) 123-4567">
        </div>
        
        <div class="form-group">
          <label for="agency">Your Department/Agency</label>
          <select id="agency" name="agency">
            <option value="">Select your department</option>
            <option value="air-force-hill">Air Force - Hill AFB</option>
            <option value="air-force-other">Air Force - Other Base</option>
            <option value="irs">Internal Revenue Service</option>
            <option value="usda">USDA Forest Service</option>
            <option value="va">Veterans Affairs</option>
            <option value="dod-army">Department of Defense - Army</option>
            <option value="dod-navy">Department of Defense - Navy</option>
            <option value="dhs">Department of Homeland Security</option>
            <option value="doi">Department of Interior</option>
            <option value="other">Other Federal Agency</option>
          </select>
        </div>
        
        <button type="submit" class="federal-submit-btn">
          Reserve My FREE Seat Now
        </button>
        
        <input type="hidden" name="source" value="federal-benefits-workshop-april-9-2026">
        <input type="hidden" name="workshop_date" value="2026-04-09">
        <input type="hidden" name="lead_type" value="federal-workshop-registration">
        <input type="hidden" name="utm_campaign" value="hill-afb-federal-workshop">
      </form>
      
      <div class="form-footer">
        <div class="contact-info">
          <p>Questions? Call <strong>(801) 210-2800</strong></p>
          <p>Or text "FEDERAL" to <strong>(801) 210-2800</strong></p>
        </div>
      </div>
    </div>
  </div>
</section>

{# Social Proof Section #}
<section class="social-proof-section">
  <div class="container">
    <div class="social-stats">
      <div class="stat-item">
        <span class="stat-number">1,200+</span>
        <div class="stat-label">Federal Employees Helped</div>
      </div>
      <div class="stat-item">
        <span class="stat-number">$300M+</span>
        <div class="stat-label">Federal Assets Managed</div>
      </div>
      <div class="stat-item">
        <span class="stat-number">25+</span>
        <div class="stat-label">Years Experience</div>
      </div>
      <div class="stat-item">
        <span class="stat-number">5.0</span>
        <div class="stat-label">Google Rating</div>
      </div>
    </div>
  </div>
</section>

{# Value Proposition Section #}
<section class="value-section">
  <div class="container">
    <div class="value-header">
      <h2>What You'll Learn at This Exclusive Workshop</h2>
      <p class="description">Our proven system to maximize every federal benefit for a secure, worry-free retirement.</p>
    </div>
    
    <div class="value-grid">
      <div class="value-card">
        <div class="value-icon">📈</div>
        <h3>FERS Pension Optimization</h3>
        <p>Calculate your optimal retirement date and discover advanced strategies to maximize your FERS pension payments throughout retirement.</p>
      </div>
      
      <div class="value-card">
        <div class="value-icon">💰</div>
        <h3>TSP Maximization Strategies</h3>
        <p>Learn optimal contribution amounts, investment allocation strategies, and withdrawal sequences to get the most from your TSP.</p>
      </div>
      
      <div class="value-card">
        <div class="value-icon">🏥</div>
        <h3>Healthcare & Medicare Planning</h3>
        <p>Navigate FEHB continuation, Medicare integration, and supplement insurance to ensure comprehensive coverage in retirement.</p>
      </div>
      
      <div class="value-card">
        <div class="value-icon">🛡️</div>
        <h3>FEGLI Life Insurance Analysis</h3>
        <p>Understand your life insurance options and make informed decisions about continuing coverage versus private alternatives.</p>
      </div>
      
      <div class="value-card">
        <div class="value-icon">📊</div>
        <h3>Social Security Integration</h3>
        <p>Coordinate Social Security benefits with your federal pension and TSP to maximize total retirement income streams.</p>
      </div>
      
      <div class="value-card">
        <div class="value-icon">📋</div>
        <h3>Tax Minimization Strategies</h3>
        <p>Strategic withdrawal sequencing from different account types to minimize taxes and maximize your retirement spending power.</p>
      </div>
    </div>
  </div>
</section>

{# Host Section #}
<section class="host-section">
  <div class="container">
    <div class="host-content">
      <div class="host-image">
        <img src="/assets/images/photos/mike-tvheader.webp" alt="Mike Stevens, CFP® - Capital Wealth Advisors President" loading="lazy">
      </div>
      
      <div class="host-details">
        <p class="host-label">Workshop Host</p>
        <h3>Mike Stevens, CFP®</h3>
        <p class="host-title">President & Founder, Capital Wealth Advisors</p>
        
        <div class="credentials">
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>25+ years specializing in federal employee benefits</span>
          </div>
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>Host of Utah's #1 retirement radio show "Retire Right Radio"</span>
          </div>
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>Author of "Retire Right, Retire Now"</span>
          </div>
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>Helped 1,200+ federal employees maximize their benefits</span>
          </div>
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>Manages over $300M in federal retirement assets</span>
          </div>
          <div class="credential-item">
            <span class="credential-check">✓</span>
            <span>Series 66 Investment Adviser Representative</span>
          </div>
        </div>
        
        <div class="host-quote">
          "Federal employees have incredible retirement benefits that most financial advisors simply don't understand. My mission is to help every federal employee maximize these benefits and retire with complete confidence. In 25 years, I've seen too many federal employees miss out on thousands in benefits simply because they didn't know the rules."
        </div>
      </div>
    </div>
  </div>
</section>

{# Final Urgency CTA Section #}
<section class="urgency-cta">
  <div class="container">
    <div class="urgency-content">
      <h2>Don't Miss This Opportunity</h2>
      <p class="urgency-text">This exclusive workshop is specifically designed for Hill AFB federal employees and is completely FREE. Seating is extremely limited.</p>
      
      <div class="urgency-details">
        <h3>🎯 Limited Seating - Only 50 Spots Available</h3>
        <div class="event-summary">
          📅 <strong>Thursday, April 9, 2026</strong><br>
          ⏰ <strong>9:00 AM - 11:00 AM (Light Breakfast Provided)</strong><br>
          📍 <strong>Weber State University - Davis Campus</strong><br>
          💡 <strong>Learn strategies that could add $50,000+ to your retirement</strong>
        </div>
      </div>
      
      <div class="cta-buttons">
        <a href="#federal-workshop-form" class="cta-primary">Reserve My Seat - FREE</a>
        <a href="tel:8012102800" class="cta-secondary">
          📞 Call (801) 210-2800
        </a>
      </div>
    </div>
  </div>
</section>