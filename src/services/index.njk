---
layout: layouts/base-finpay.njk
title: "Financial Advisory Services | Retirement Planning | Capital Wealth"
description: "Comprehensive retirement planning services including tax-efficient strategies, portfolio management, Social Security optimization, Medicare planning, and estate planning. Serving Utah families since 2010."
breadcrumbs:
  - { name: "Home", url: "/" }
  - { name: "Services", url: "/services/" }
---

<style>
/* ═══════════════════════════════════════════════════════════
   SERVICES HUB — 3-PILLAR OVERVIEW
   ═══════════════════════════════════════════════════════════ */

/* ---------- HERO ---------- */
.svch-hero {
  background: var(--surface);
  padding: clamp(4rem, 8vw, 7rem) 0 clamp(3rem, 6vw, 5rem);
  text-align: center;
}

.svch-hero-inner {
  max-width: 760px;
  margin: 0 auto;
  padding: 0 var(--space-6, 1.5rem);
}

.svch-eyebrow {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--secondary);
  margin-bottom: 1.25rem;
  display: block;
}

.svch-hero-title {
  font-family: var(--font-serif-display);
  font-size: clamp(2.4rem, 5vw, 3.6rem);
  font-weight: 300;
  font-style: italic;
  line-height: 1.1;
  color: var(--primary);
  margin: 0 0 1.25rem;
}

.svch-hero-sub {
  font-family: var(--font-body);
  font-size: 1.1rem;
  font-weight: 400;
  line-height: 1.65;
  color: var(--on-surface-variant);
  max-width: 580px;
  margin: 0 auto;
}

/* ---------- SECTION SHARED ---------- */
.svch-section {
  padding: clamp(4rem, 7vw, 6rem) 0;
}

.svch-container {
  max-width: 1140px;
  margin: 0 auto;
  padding: 0 var(--space-6, 1.5rem);
}

.svch-section--surface { background: var(--surface); }
.svch-section--white   { background: #fff; }
.svch-section--navy    { background: var(--primary); }

.svch-section-eyebrow {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  color: var(--secondary);
  margin-bottom: 0.75rem;
  display: block;
}

.svch-section-title {
  font-family: var(--font-serif-display);
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 600;
  line-height: 1.15;
  color: var(--primary);
  margin: 0 0 1rem;
}

.svch-section-body {
  font-family: var(--font-body);
  font-size: 1.05rem;
  line-height: 1.7;
  color: var(--on-surface-variant);
  max-width: 560px;
}

/* ---------- PILLAR 1 — TRADITIONAL RETIREMENT ---------- */
.svch-trad-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: start;
}

.svch-trad-left {
  border-left: 3px solid var(--secondary);
  padding-left: 1.75rem;
}

.svch-check-list {
  list-style: none;
  padding: 0;
  margin: 1.75rem 0 0;
  display: grid;
  gap: 0.65rem;
}

.svch-check-list li {
  font-family: var(--font-body);
  font-size: 0.95rem;
  color: var(--on-surface);
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.svch-check-list li::before {
  content: "\2713";
  color: var(--secondary);
  font-weight: 700;
  font-size: 1rem;
  flex-shrink: 0;
}

.svch-questions-card {
  background: #fff;
  border: 1px solid var(--outline-variant, #dde3ea);
  border-radius: 16px;
  padding: 2rem;
}

.svch-questions-card h3 {
  font-family: var(--font-body);
  font-size: 0.8rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--on-surface-variant);
  margin: 0 0 1.25rem;
}

.svch-q-link {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.85rem 0;
  border-bottom: 1px solid var(--outline-variant, #dde3ea);
  text-decoration: none;
  font-family: var(--font-body);
  font-size: 0.92rem;
  font-weight: 500;
  color: var(--primary);
  transition: color 0.2s;
}

.svch-q-link:last-child { border-bottom: none; }
.svch-q-link:hover { color: var(--secondary); }

.svch-q-link::after {
  content: "\2192";
  font-size: 1.1rem;
  flex-shrink: 0;
  transition: transform 0.2s;
}

.svch-q-link:hover::after { transform: translateX(3px); }

/* ---------- PILLAR 2 — FEDERAL BENEFITS ---------- */
.svch-fed-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
}

.svch-fed-img-wrap {
  position: relative;
  border-radius: 16px;
  overflow: hidden;
}

.svch-fed-img-wrap img {
  width: 100%;
  display: block;
  border-radius: 16px;
  filter: saturate(1);
  transition: filter 0.4s ease;
}

.svch-fed-img-wrap:hover img {
  filter: grayscale(1);
}

.svch-fed-book-overlay {
  position: absolute;
  bottom: 1.25rem;
  left: 1.25rem;
  right: 1.25rem;
  background: rgba(1, 15, 38, 0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-radius: 12px;
  padding: 1rem 1.25rem;
  color: #fff;
}

.svch-fed-book-overlay small {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  opacity: 0.7;
  display: block;
  margin-bottom: 0.3rem;
}

.svch-fed-book-overlay span {
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 600;
}

.svch-fed-badge {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: 0.65rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.14em;
  background: var(--secondary);
  color: var(--primary);
  padding: 0.3rem 0.75rem;
  border-radius: 6px;
  margin-bottom: 1rem;
}

.svch-fed-sub-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  margin-top: 1.75rem;
}

.svch-fed-sub-card {
  background: var(--surface);
  border: 1px solid var(--outline-variant, #dde3ea);
  border-radius: 12px;
  padding: 1.25rem;
}

.svch-fed-sub-card h4 {
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 700;
  color: var(--primary);
  margin: 0 0 0.35rem;
}

.svch-fed-sub-card p {
  font-family: var(--font-body);
  font-size: 0.82rem;
  color: var(--on-surface-variant);
  margin: 0;
  line-height: 1.5;
}

.svch-fed-link {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  font-family: var(--font-body);
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--secondary);
  text-decoration: none;
  margin-top: 1.5rem;
  transition: gap 0.2s;
}

.svch-fed-link:hover { gap: 0.7rem; }

/* ---------- PILLAR 3 — SDBA ---------- */
.svch-sdba-title {
  font-family: var(--font-serif-display);
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 600;
  line-height: 1.15;
  color: #fff;
  margin: 0 0 1rem;
}

.svch-sdba-body {
  font-family: var(--font-body);
  font-size: 1.05rem;
  line-height: 1.7;
  color: rgba(255,255,255,0.7);
  max-width: 560px;
  margin-bottom: 2.5rem;
}

.svch-sdba-cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.25rem;
  margin-bottom: 2.5rem;
}

.svch-sdba-card {
  background: rgba(255,255,255,0.06);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 14px;
  padding: 1.5rem;
}

.svch-sdba-card h4 {
  font-family: var(--font-body);
  font-size: 0.9rem;
  font-weight: 700;
  color: #fff;
  margin: 0 0 0.5rem;
}

.svch-sdba-card p {
  font-family: var(--font-body);
  font-size: 0.82rem;
  color: rgba(255,255,255,0.6);
  margin: 0;
  line-height: 1.55;
}

.svch-sdba-link {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  font-family: var(--font-body);
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--secondary);
  text-decoration: none;
  transition: gap 0.2s;
}

.svch-sdba-link:hover { gap: 0.7rem; }

/* ---------- MEDIA LOGOS ---------- */
.svch-media {
  background: var(--surface-container-low);
  padding: clamp(2.5rem, 4vw, 3.5rem) 0;
}

.svch-media-inner {
  max-width: 960px;
  margin: 0 auto;
  padding: 0 var(--space-6, 1.5rem);
  text-align: center;
}

.svch-media-label {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--on-surface-variant);
  margin-bottom: 1.5rem;
  display: block;
}

.svch-media-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(2rem, 5vw, 3.5rem);
  flex-wrap: wrap;
}

.svch-media-row img {
  height: 28px;
  width: auto;
  opacity: 0.5;
  filter: grayscale(1);
  transition: opacity 0.3s, filter 0.3s;
}

.svch-media-row img:hover {
  opacity: 1;
  filter: grayscale(0);
}

/* ---------- FINAL CTA ---------- */
.svch-cta {
  background: var(--surface);
  padding: clamp(4rem, 8vw, 6rem) 0;
  text-align: center;
}

.svch-cta-inner {
  max-width: 620px;
  margin: 0 auto;
  padding: 0 var(--space-6, 1.5rem);
}

.svch-cta-title {
  font-family: var(--font-serif-display);
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 300;
  font-style: italic;
  line-height: 1.15;
  color: var(--primary);
  margin: 0 0 1.5rem;
}

.svch-cta-btns {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.svch-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.85rem 2rem;
  border-radius: 14px;
  text-decoration: none;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.9rem;
  transition: all 0.3s ease;
}

.svch-btn--primary {
  background: var(--primary);
  color: #fff;
  border: none;
}

.svch-btn--primary:hover {
  opacity: 0.9;
  transform: translateY(-1px);
}

.svch-btn--secondary {
  background: transparent;
  color: var(--primary);
  border: 1.5px solid var(--primary);
}

.svch-btn--secondary:hover {
  background: var(--primary);
  color: #fff;
}

/* ---------- RESPONSIVE ---------- */
@media (max-width: 768px) {
  .svch-trad-grid,
  .svch-fed-grid {
    grid-template-columns: 1fr;
  }

  .svch-sdba-cards {
    grid-template-columns: 1fr;
  }

  .svch-fed-sub-grid {
    grid-template-columns: 1fr;
  }
}
</style>

<!-- ════════════ HERO ════════════ -->
<section class="svch-hero">
  <div class="svch-hero-inner">
    <span class="svch-eyebrow">Fiduciary Grade Advisory</span>
    <h1 class="svch-hero-title">Curating Wealth for Life's Next Chapter</h1>
    <p class="svch-hero-sub">
      Move beyond transactional planning. Our advisory practice integrates tax strategy,
      investment management, and risk mitigation into a single, coordinated wealth plan
      designed to outlast every market cycle.
    </p>
  </div>
</section>

<!-- ════════════ PILLAR 1 — TRADITIONAL RETIREMENT ════════════ -->
<section class="svch-section svch-section--surface">
  <div class="svch-container">
    <div class="svch-trad-grid">

      <!-- Left: Content -->
      <div class="svch-trad-left">
        <span class="svch-section-eyebrow">Pillar One</span>
        <h2 class="svch-section-title">Traditional Retirement Planning</h2>
        <p class="svch-section-body">
          Our Retirement Money Map brings clarity to every decision you face in the decade
          before and after retirement. From tax-bracket management to estate coordination,
          every piece works together.
        </p>
        <ul class="svch-check-list">
          <li>Tax Planning &amp; Bracket Management</li>
          <li>Social Security Optimization</li>
          <li>Estate &amp; Legacy Planning</li>
          <li>Medicare &amp; IRMAA Strategy</li>
        </ul>
      </div>

      <!-- Right: Common Questions -->
      <div class="svch-questions-card">
        <h3>Common Questions We Answer</h3>
        <a href="/services/social-security/" class="svch-q-link">When should I claim Social Security?</a>
        <a href="/services/401k-ira-rollover/" class="svch-q-link">Should I roll over my 401(k)?</a>
        <a href="/services/medicare-irmaa/" class="svch-q-link">How do I avoid IRMAA surcharges?</a>
        <a href="/services/roth-conversions/" class="svch-q-link">Is a Roth conversion right for me?</a>
      </div>

    </div>
  </div>
</section>

<!-- ════════════ PILLAR 2 — FEDERAL BENEFITS ════════════ -->
<section class="meet-mike" style="padding: 4rem 2rem; background: var(--surface);">
    <div class="mike-content" style="max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1.2fr; gap: 3rem; align-items: center;">
        <div class="mike-photo-container" style="position: relative;">
            <img src="/assets/images/mike-stevens.jpg" alt="Mike Stevens, Founder of Capital Wealth" style="width: 100%; border-radius: 16px; filter: grayscale(0.15); box-shadow: 0 20px 50px rgba(0, 56, 112, 0.08);">
            <div style="position: absolute; bottom: -1.5rem; right: -1.5rem; background: rgba(255, 255, 255, 0.5); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px); padding: 1.5rem 2rem; border-radius: 16px; border: 1px solid rgba(255, 255, 255, 0.35); max-width: 300px; box-shadow: 0 8px 32px rgba(0, 56, 112, 0.06);">
                <p style="font-family: var(--font-serif-display); font-style: italic; margin: 0; color: var(--primary); font-size: 1rem; line-height: 1.5;">"Federal employees work their entire lives for their country. My mission is to ensure their country's benefits system works just as hard for them in retirement."</p>
            </div>
        </div>

        <div>
            <span style="font-family: var(--font-mono); font-size: 0.65rem; font-weight: 500; letter-spacing: 0.15em; text-transform: uppercase; color: var(--on-surface-variant); display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.35rem 0.9rem; background: rgba(255, 255, 255, 0.5); backdrop-filter: blur(20px); border: 1px solid rgba(255, 255, 255, 0.3); border-radius: 16px; margin-bottom: 1rem;"><span style="width:6px;height:6px;border-radius:50%;background:var(--secondary);display:inline-block;"></span>THE SPECIALIST</span>
            <h2 style="font-family: var(--font-serif-display); font-size: 2.5rem; font-style: italic; font-weight: 300; color: var(--primary); margin-bottom: 0.35rem;">Authentic Fiduciary Expertise for Federal Employees</h2>
            <p style="font-size: 1rem; color: var(--on-surface-variant); margin-bottom: 1.5rem;">Led by Mike Stevens, Author of <em>The Federal Benefits Gap</em></p>

            <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.75rem; margin-bottom: 1.5rem;">
                <div style="font-size: 0.85rem; color: var(--on-surface-variant); display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.4rem 0.85rem; background: rgba(255, 255, 255, 0.5); backdrop-filter: blur(12px); border: 1px solid rgba(194, 198, 210, 0.25); border-radius: 16px;"><span style="width:5px;height:5px;border-radius:50%;background:var(--secondary);flex-shrink:0;display:inline-block;"></span>Host, Retire Right Radio</div>
                <div style="font-size: 0.85rem; color: var(--on-surface-variant); display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.4rem 0.85rem; background: rgba(255, 255, 255, 0.5); backdrop-filter: blur(12px); border: 1px solid rgba(194, 198, 210, 0.25); border-radius: 16px;"><span style="width:5px;height:5px;border-radius:50%;background:var(--secondary);flex-shrink:0;display:inline-block;"></span>Author, <em>The Federal Benefits Gap</em></div>
            </div>

            <p style="font-size: 0.95rem; line-height: 1.7; color: var(--on-surface); margin-bottom: 1.5rem;">Mike Stevens has dedicated two decades to navigating the intricate corridors of government retirement systems. His approach doesn't just manage assets — it orchestrates a comprehensive financial legacy. Unlike generic, seminar-driven models that treat federal employees as leads, Capital Wealth provides deep-dive fiduciary expertise.</p>
            
            <a href="/services/federal-benefits/" style="display: inline-block; padding: 0.85rem 2rem; border-radius: 16px; text-decoration: none; font-weight: 600; font-size: 0.9rem; background: rgba(0, 56, 112, 0.85); backdrop-filter: blur(20px); border: 1px solid rgba(0, 56, 112, 0.3); color: white; transition: all 0.3s ease;">Explore Federal Benefits</a>
        </div>
    </div>
</section>

<!-- ════════════ PILLAR 3 — SDBA ════════════ -->
<section class="svch-section svch-section--navy">
  <div class="svch-container">
    <span class="svch-section-eyebrow" style="color:var(--secondary);">The Opportunity</span>
    <h2 class="svch-sdba-title">Unlock Your Current Workplace 401(k)</h2>
    <p class="svch-sdba-body">
      A Self-Directed Brokerage Account lets you invest beyond your plan's default menu —
      without leaving your employer. We manage the window so you get institutional-quality
      strategy inside your existing retirement plan.
    </p>

    <div class="svch-sdba-cards">
      <div class="svch-sdba-card">
        <h4>Broader Access</h4>
        <p>Move beyond target-date funds to a diversified portfolio of ETFs, individual equities, and alternatives.</p>
      </div>
      <div class="svch-sdba-card">
        <h4>Active Management</h4>
        <p>We monitor and rebalance your brokerage window alongside your total wealth picture.</p>
      </div>
      <div class="svch-sdba-card">
        <h4>No Plan Change</h4>
        <p>Your account stays with your current employer plan — no rollover, no disruption to payroll contributions.</p>
      </div>
    </div>

    <a href="/services/sdba/" class="svch-sdba-link">Learn about SDBA management &rarr;</a>
  </div>
</section>

<!-- ════════════ MEDIA LOGOS ════════════ -->
<section class="svch-media">
  <div class="svch-media-inner">
    <span class="svch-media-label">As Featured In</span>
    <div class="svch-media-row">
      <img src="/assets/images/media/ksl.png" alt="KSL" loading="lazy" />
      <img src="/assets/images/media/fortune.png" alt="Fortune" loading="lazy" />
      <img src="/assets/images/media/cnn-money.png" alt="CNN Money" loading="lazy" />
      <img src="/assets/images/media/knrs-2.png" alt="KNRS" loading="lazy" />
      <img src="/assets/images/media/newstalk.png" alt="News Talk" loading="lazy" />
      <img src="/assets/images/media/KIDLogo-small.png" alt="KID" loading="lazy" />
    </div>
  </div>
</section>

<!-- ════════════ FINAL CTA ════════════ -->
<section class="svch-cta">
  <div class="svch-cta-inner">
    <h2 class="svch-cta-title">Ready to curate your wealth journey?</h2>
    <div class="svch-cta-btns">
      <a href="/contact/" class="svch-btn svch-btn--primary">Start a Conversation</a>
      <a href="/book/" class="svch-btn svch-btn--secondary">Book a Consultation</a>
    </div>
  </div>
</section>
