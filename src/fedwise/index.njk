---
layout: layouts/fedwise-base.njk
permalink: "/fedwise/"
title: "FedWise — Federal benefits, decoded."
description: "A guided walkthrough of FERS, TSP, FEHB, Social Security, and every retirement decision you'll actually face — built with a 25-year federal benefits expert."
noindex: true
---

<style>
  .fw-home {
    --section-pad: 6rem;
  }

  /* ---------- HERO ---------- */
  .fw-hero {
    position: relative;
    padding: 5.5rem 0 6rem;
    background:
      radial-gradient(circle at 12% 8%, rgba(184,128,12,0.10), transparent 45%),
      radial-gradient(circle at 88% 6%, rgba(10,49,97,0.07), transparent 50%),
      var(--paper);
    overflow: hidden;
  }
  .fw-hero::before {
    content: ""; position: absolute; left: 0; right: 0; top: 0; height: 1px;
    background: linear-gradient(90deg, transparent, var(--rule), transparent);
  }
  .fw-hero-inner { display: grid; grid-template-columns: 1.15fr 1fr; gap: 4rem; align-items: center; }
  .fw-eyebrow {
    display: inline-flex; align-items: center; gap: 0.55rem;
    font-size: 0.72rem; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 1.4rem;
  }
  .fw-eyebrow::before {
    content: ""; display: inline-block; width: 24px; height: 1.5px; background: var(--gold);
  }
  .fw-hero h1 {
    font-family: var(--font-display); font-weight: 600;
    font-size: clamp(2.6rem, 5.8vw, 4.6rem); line-height: 1.02; letter-spacing: -0.02em;
    margin: 0 0 1.5rem; color: var(--ink);
  }
  .fw-hero h1 em { font-style: italic; font-weight: 500; color: var(--gold); }
  .fw-hero h1 .underline {
    background-image: linear-gradient(transparent 80%, rgba(184,128,12,0.25) 80%);
    padding: 0 0.05em;
  }
  .fw-hero .lede {
    font-size: 1.18rem; line-height: 1.62; color: var(--ink-2); margin: 0 0 2rem; max-width: 38rem;
  }
  .fw-hero .ctas { display: flex; flex-wrap: wrap; gap: 0.85rem; margin-bottom: 2.4rem; }
  .fw-hero-meta { display: flex; flex-wrap: wrap; gap: 1.5rem 2.5rem; font-size: 0.86rem; color: var(--muted); }
  .fw-hero-meta strong { color: var(--ink); font-weight: 600; }
  .fw-hero-meta span::before { content: "✓"; color: var(--gold); font-weight: 700; margin-right: 0.4rem; }

  /* Hero visual (right side) */
  .fw-hero-card {
    position: relative;
    background: #fff; border-radius: 22px; padding: 0;
    box-shadow: 0 30px 80px -20px rgba(15,26,42,0.22);
    border: 1px solid var(--rule);
    overflow: hidden;
  }
  .fw-hero-card-head {
    background: var(--ink); color: #fff; padding: 1.1rem 1.4rem;
    display: flex; align-items: center; gap: 0.7rem;
    font-family: var(--font-sans); font-weight: 600; font-size: 0.92rem;
  }
  .fw-hero-card-head .dots { display: flex; gap: 0.35rem; margin-right: 0.5rem; }
  .fw-hero-card-head .dots span { width: 9px; height: 9px; border-radius: 50%; background: rgba(255,255,255,0.18); }
  .fw-hero-card-head .label { color: var(--gold-soft); font-size: 0.72rem; letter-spacing: 0.12em; text-transform: uppercase; font-weight: 700; }
  .fw-hero-card-body { padding: 1.5rem 1.5rem 1.8rem; }
  .fw-hero-card-q { font-family: var(--font-display); font-size: 1.45rem; line-height: 1.25; color: var(--ink); margin: 0 0 1.1rem; font-weight: 600; }
  .fw-hero-card-q em { font-style: italic; color: var(--gold); }
  .fw-path { display: flex; flex-direction: column; gap: 0.55rem; }
  .fw-path-item {
    display: flex; align-items: center; gap: 0.85rem;
    padding: 0.85rem 1rem; background: var(--paper); border-radius: 12px;
    border: 1px solid var(--rule); font-size: 0.92rem; color: var(--ink-2); font-weight: 500;
    transition: background .15s ease, transform .15s ease;
  }
  .fw-path-item:hover { background: var(--paper-2); transform: translateX(3px); cursor: default; }
  .fw-path-item .num { width: 28px; height: 28px; border-radius: 8px; background: var(--ink); color: #fff; display: flex; align-items: center; justify-content: center; font-size: 0.78rem; font-weight: 700; flex: none; }
  .fw-path-item.active { background: #FFF8E6; border-color: var(--gold-soft); }
  .fw-path-item.active .num { background: var(--gold); }
  .fw-path-arrow { margin-left: auto; color: var(--muted); transition: color .15s ease, transform .15s ease; }
  .fw-path-item:hover .fw-path-arrow { color: var(--ink); transform: translateX(2px); }

  /* ---------- TRUST BAND ---------- */
  .fw-trust {
    background: var(--ink); color: #fff; padding: 3rem 0;
    position: relative;
  }
  .fw-trust-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2rem; }
  .fw-stat { text-align: center; }
  .fw-stat .num {
    font-family: var(--font-display); font-size: clamp(2.4rem, 4vw, 3.2rem); font-weight: 600;
    color: var(--gold-soft); line-height: 1; margin-bottom: 0.55rem; letter-spacing: -0.01em;
  }
  .fw-stat .num em { font-style: italic; }
  .fw-stat .label { font-size: 0.82rem; color: rgba(255,255,255,0.72); letter-spacing: 0.04em; line-height: 1.5; max-width: 200px; margin: 0 auto; }
  .fw-stat .label strong { color: #fff; font-weight: 600; }

  /* ---------- SECTION SCAFFOLD ---------- */
  section.fw-section { padding: var(--section-pad) 0; }
  .fw-section-head { text-align: center; max-width: 720px; margin: 0 auto 4rem; }
  .fw-section-eyebrow { font-size: 0.74rem; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase; color: var(--gold); margin-bottom: 1rem; display: block; }
  .fw-section-title { font-family: var(--font-display); font-size: clamp(2rem, 4vw, 3rem); font-weight: 600; line-height: 1.1; letter-spacing: -0.015em; color: var(--ink); margin: 0 0 1.1rem; }
  .fw-section-title em { font-style: italic; color: var(--gold); }
  .fw-section-sub { font-size: 1.06rem; line-height: 1.6; color: var(--ink-2); margin: 0; }

  /* ---------- WALKTHROUGH (situations grid) ---------- */
  .fw-walk { background: var(--paper-2); position: relative; }
  .fw-walk::before, .fw-walk::after {
    content: ""; position: absolute; left: 0; right: 0; height: 1px;
    background: var(--rule);
  }
  .fw-walk::before { top: 0; } .fw-walk::after { bottom: 0; }
  .fw-walk-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
  .fw-walk-card {
    background: #fff; border-radius: 18px; padding: 2rem 1.7rem 1.8rem;
    border: 1px solid var(--rule);
    transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
    display: flex; flex-direction: column; gap: 0.9rem;
    text-decoration: none; color: inherit;
  }
  .fw-walk-card:hover { transform: translateY(-4px); box-shadow: var(--shadow); border-color: var(--gold-soft); }
  .fw-walk-card .ic {
    width: 44px; height: 44px; border-radius: 12px; background: var(--paper);
    display: flex; align-items: center; justify-content: center;
    font-family: var(--font-display); font-style: italic; font-weight: 600; font-size: 1.3rem; color: var(--gold);
    border: 1px solid var(--rule);
  }
  .fw-walk-card h3 { font-family: var(--font-display); font-weight: 600; font-size: 1.5rem; line-height: 1.2; margin: 0; color: var(--ink); letter-spacing: -0.01em; }
  .fw-walk-card p { font-size: 0.94rem; line-height: 1.55; color: var(--muted); margin: 0; }
  .fw-walk-card .go { font-size: 0.85rem; font-weight: 600; color: var(--ink); margin-top: auto; padding-top: 0.5rem; display: inline-flex; align-items: center; gap: 0.4rem; }
  .fw-walk-card:hover .go { color: var(--gold); }
  .fw-walk-card:hover .go .fw-arrow { transform: translateX(4px); }

  /* ---------- TOPICS GRID ---------- */
  .fw-topics { background: var(--paper); }
  .fw-topics-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; }
  .fw-topic {
    background: #fff; border: 1px solid var(--rule); border-radius: 14px;
    padding: 1.4rem 1.2rem; display: flex; flex-direction: column; gap: 0.6rem;
    text-decoration: none; color: inherit;
    transition: border-color .15s ease, transform .15s ease;
    min-height: 130px;
  }
  .fw-topic:hover { border-color: var(--ink); transform: translateY(-2px); }
  .fw-topic .tag { font-size: 0.68rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--gold); }
  .fw-topic h4 { font-family: var(--font-display); font-weight: 600; font-size: 1.18rem; margin: 0; color: var(--ink); line-height: 1.2; }
  .fw-topic p { font-size: 0.82rem; line-height: 1.45; color: var(--muted); margin: 0; }

  /* ---------- HOW IT WORKS ---------- */
  .fw-how { background: var(--paper-2); position: relative; }
  .fw-how::before, .fw-how::after { content: ""; position: absolute; left: 0; right: 0; height: 1px; background: var(--rule); }
  .fw-how::before { top: 0; } .fw-how::after { bottom: 0; }
  .fw-steps { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; position: relative; }
  .fw-step { text-align: left; padding: 0 0.5rem; }
  .fw-step .step-num {
    font-family: var(--font-display); font-style: italic; font-weight: 600;
    font-size: 4rem; line-height: 1; color: var(--gold-soft); margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }
  .fw-step h3 { font-family: var(--font-display); font-weight: 600; font-size: 1.6rem; margin: 0 0 0.7rem; color: var(--ink); line-height: 1.15; }
  .fw-step p { font-size: 1rem; line-height: 1.6; color: var(--ink-2); margin: 0; }

  /* ---------- TESTIMONIAL ---------- */
  .fw-test { padding: var(--section-pad) 0; background: var(--ink); color: #fff; position: relative; overflow: hidden; }
  .fw-test::before {
    content: "❝"; position: absolute; top: -1rem; left: 50%; transform: translateX(-50%);
    font-family: var(--font-display); font-size: 22rem; line-height: 1; color: rgba(184,128,12,0.07); pointer-events: none;
  }
  .fw-test-inner { max-width: 880px; margin: 0 auto; text-align: center; position: relative; }
  .fw-test blockquote {
    font-family: var(--font-display); font-style: italic; font-weight: 500;
    font-size: clamp(1.55rem, 3vw, 2.2rem); line-height: 1.35; margin: 0 0 2rem;
    color: #fff; letter-spacing: -0.005em;
  }
  .fw-test blockquote .pull { color: var(--gold-soft); }
  .fw-test .att {
    display: flex; align-items: center; justify-content: center; gap: 0.85rem; font-size: 0.9rem;
  }
  .fw-test .att .avatar { width: 44px; height: 44px; border-radius: 50%; background: var(--gold); color: #fff; display: flex; align-items: center; justify-content: center; font-family: var(--font-display); font-weight: 600; font-style: italic; }
  .fw-test .att .who { text-align: left; }
  .fw-test .att .who strong { color: #fff; font-weight: 600; display: block; }
  .fw-test .att .who span { color: rgba(255,255,255,0.66); font-size: 0.82rem; }

  /* ---------- FINAL CTA ---------- */
  .fw-final {
    background: var(--paper); padding: 5rem 0 6rem; text-align: center;
  }
  .fw-final-inner {
    max-width: 760px; margin: 0 auto;
    background: linear-gradient(135deg, #fff 0%, #FFF8E6 100%);
    border: 1px solid var(--gold-soft); border-radius: 24px;
    padding: 3.5rem 3rem;
    box-shadow: var(--shadow);
  }
  .fw-final h2 { font-family: var(--font-display); font-weight: 600; font-size: clamp(2rem, 4vw, 2.8rem); line-height: 1.1; letter-spacing: -0.015em; margin: 0 0 1rem; color: var(--ink); }
  .fw-final h2 em { font-style: italic; color: var(--gold); }
  .fw-final p { font-size: 1.06rem; line-height: 1.6; color: var(--ink-2); margin: 0 0 2rem; }
  .fw-final .ctas { display: flex; flex-wrap: wrap; gap: 0.85rem; justify-content: center; }

  /* ---------- responsive ---------- */
  @media (max-width: 980px) {
    .fw-home { --section-pad: 4.5rem; }
    .fw-hero-inner { grid-template-columns: 1fr; }
    .fw-trust-grid { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
    .fw-walk-grid { grid-template-columns: 1fr 1fr; }
    .fw-topics-grid { grid-template-columns: 1fr 1fr; }
    .fw-steps { grid-template-columns: 1fr; gap: 2.5rem; }
  }
  @media (max-width: 560px) {
    .fw-home { --section-pad: 3.5rem; }
    .fw-walk-grid, .fw-topics-grid { grid-template-columns: 1fr; }
    .fw-trust-grid { grid-template-columns: 1fr; }
    .fw-final-inner { padding: 2.5rem 1.5rem; }
  }
</style>

<div class="fw-home">

  <!-- ============== HERO ============== -->
  <section class="fw-hero">
    <div class="fw-wrap fw-hero-inner">
      <div>
        <span class="fw-eyebrow">For federal employees</span>
        <h1>Federal benefits,<br><em>decoded</em><span style="color:var(--ink);">.</span></h1>
        <p class="lede">A guided walkthrough of every retirement decision you&rsquo;ll actually face &mdash; FERS, TSP, FEHB, Social Security, survivor benefits, VERA/VSIP &mdash; built with a federal benefits expert who&rsquo;s been doing this for 25 years.</p>
        <div class="ctas">
          <a href="/fedwise/walkthrough/" class="fw-btn fw-btn-primary">Start your walkthrough <span class="fw-arrow">→</span></a>
          <a href="#topics" class="fw-btn fw-btn-ghost">Browse topics</a>
        </div>
        <div class="fw-hero-meta">
          <span><strong>Free</strong> &nbsp;to use</span>
          <span><strong>No sales pitch</strong></span>
          <span>Real experts on chat, <strong>not a bot</strong></span>
        </div>
      </div>

      <div class="fw-hero-card" aria-hidden="true">
        <div class="fw-hero-card-head">
          <div class="dots"><span></span><span></span><span></span></div>
          <div class="label">Your walkthrough</div>
        </div>
        <div class="fw-hero-card-body">
          <h3 class="fw-hero-card-q">Where are you in your federal career, <em>really</em>?</h3>
          <div class="fw-path">
            <div class="fw-path-item">
              <span class="num">1</span><span>5+ years away from retirement</span>
              <span class="fw-path-arrow">→</span>
            </div>
            <div class="fw-path-item active">
              <span class="num">2</span><span>Within 5 years of retiring</span>
              <span class="fw-path-arrow">→</span>
            </div>
            <div class="fw-path-item">
              <span class="num">3</span><span>I was offered a VERA / VSIP</span>
              <span class="fw-path-arrow">→</span>
            </div>
            <div class="fw-path-item">
              <span class="num">4</span><span>Already retired &mdash; managing TSP</span>
              <span class="fw-path-arrow">→</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============== TRUST BAND ============== -->
  <section class="fw-trust">
    <div class="fw-wrap">
      <div class="fw-trust-grid">
        <div class="fw-stat">
          <div class="num">25<em>+</em></div>
          <div class="label"><strong>Years</strong><br>inside federal benefits</div>
        </div>
        <div class="fw-stat">
          <div class="num">3,200<em>+</em></div>
          <div class="label"><strong>Federal employees</strong><br>educated to date</div>
        </div>
        <div class="fw-stat">
          <div class="num">12</div>
          <div class="label"><strong>Topics</strong> covered<br>in plain English</div>
        </div>
        <div class="fw-stat">
          <div class="num">$0</div>
          <div class="label"><strong>To use it.</strong><br>Always.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============== WALKTHROUGH ============== -->
  <section class="fw-section fw-walk">
    <div class="fw-wrap">
      <div class="fw-section-head">
        <span class="fw-section-eyebrow">The walkthrough</span>
        <h2 class="fw-section-title">Pick your situation &mdash; we&rsquo;ll <em>build the path</em>.</h2>
        <p class="fw-section-sub">Most federal benefits sites dump every topic on every visitor. FedWise asks where you actually are, then walks you through only what matters for <em>you</em>.</p>
      </div>
      <div class="fw-walk-grid">
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">A</div>
          <h3>Early career</h3>
          <p>Set your TSP allocation right, understand what you&rsquo;re building, and avoid the mistakes that cost decades.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">B</div>
          <h3>5+ years from retirement</h3>
          <p>Run the real pension math, decide when you can actually retire, and pre-plan your survivor election.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">C</div>
          <h3>Within 5 years</h3>
          <p>Lock the best retirement date, finalize FEHB vs. Medicare, and time your TSP withdrawal strategy.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">D</div>
          <h3>Offered VERA / VSIP</h3>
          <p>Short decision window, big consequences. Run the numbers before you sign.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">E</div>
          <h3>Already retired</h3>
          <p>Manage your TSP draw, Roth conversions, Social Security timing, and Medicare.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
        <a href="/fedwise/walkthrough/" class="fw-walk-card">
          <div class="ic">F</div>
          <h3>Open Season</h3>
          <p>Pick the right FEHB plan in 20 minutes &mdash; without reading 200 pages of plan brochures.</p>
          <span class="go">Start here <span class="fw-arrow">→</span></span>
        </a>
      </div>
    </div>
  </section>

  <!-- ============== TOPICS ============== -->
  <section class="fw-section fw-topics" id="topics">
    <div class="fw-wrap">
      <div class="fw-section-head">
        <span class="fw-section-eyebrow">Every topic</span>
        <h2 class="fw-section-title">In <em>plain English</em>. No HR-handbook script.</h2>
        <p class="fw-section-sub">The decisions you actually have to make, explained by someone who&rsquo;s been answering them for 25 years.</p>
      </div>
      <div class="fw-topics-grid">
        <a href="#" class="fw-topic"><span class="tag">FERS</span><h4>FERS Pension</h4><p>When you can retire, the formula, sick leave, survivor election.</p></a>
        <a href="#" class="fw-topic"><span class="tag">TSP</span><h4>TSP Basics</h4><p>Funds, contributions, transfers, loans &mdash; how it actually works.</p></a>
        <a href="#" class="fw-topic"><span class="tag">TSP</span><h4>TSP in Retirement</h4><p>Generate monthly income without running out.</p></a>
        <a href="#" class="fw-topic"><span class="tag">TSP</span><h4>Roth Conversions</h4><p>New for 2026: when conversions make sense, when they don&rsquo;t.</p></a>
        <a href="#" class="fw-topic"><span class="tag">FERS</span><h4>FERS Supplement</h4><p>The bridge to Social Security at 62 &mdash; and how it ends.</p></a>
        <a href="#" class="fw-topic"><span class="tag">Income</span><h4>Social Security</h4><p>When to start, single vs. married, taxes on benefits.</p></a>
        <a href="#" class="fw-topic"><span class="tag">FEHB</span><h4>FEHB Plan Choice</h4><p>Cut through 200 pages of brochures in Open Season.</p></a>
        <a href="#" class="fw-topic"><span class="tag">Medicare</span><h4>Medicare &amp; FEHB</h4><p>The one chance you get at 65. Don&rsquo;t blow it.</p></a>
        <a href="#" class="fw-topic"><span class="tag">FEGLI</span><h4>FEGLI</h4><p>What to keep, what to cancel, what to replace.</p></a>
        <a href="#" class="fw-topic"><span class="tag">RIF</span><h4>VERA / VSIP / RIF</h4><p>Run the numbers before an offer arrives.</p></a>
        <a href="#" class="fw-topic"><span class="tag">Process</span><h4>Retirement Application</h4><p>The full process &mdash; application to final adjudication.</p></a>
        <a href="#" class="fw-topic"><span class="tag">Estate</span><h4>Survivor Benefits</h4><p>The choice you can&rsquo;t take back. Get it right.</p></a>
      </div>
    </div>
  </section>

  <!-- ============== HOW IT WORKS ============== -->
  <section class="fw-section fw-how" id="how-it-works">
    <div class="fw-wrap">
      <div class="fw-section-head">
        <span class="fw-section-eyebrow">How it works</span>
        <h2 class="fw-section-title">Three steps to a plan that&rsquo;s actually <em>yours</em>.</h2>
      </div>
      <div class="fw-steps">
        <div class="fw-step">
          <div class="step-num">01</div>
          <h3>Pick your situation</h3>
          <p>Tell us where you are in your federal career. We skip everything that doesn&rsquo;t apply to you.</p>
        </div>
        <div class="fw-step">
          <div class="step-num">02</div>
          <h3>Walk through your decisions</h3>
          <p>Plain-English explanations, real numbers, and the trade-offs nobody else lays out for you.</p>
        </div>
        <div class="fw-step">
          <div class="step-num">03</div>
          <h3>Get expert help when you need it</h3>
          <p>Stuck on a specific decision? Open the chat and talk to a real federal benefits specialist. No bots.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============== TESTIMONIAL ============== -->
  <section class="fw-test">
    <div class="fw-wrap fw-test-inner">
      <blockquote>
        &ldquo;I&rsquo;ve been to three different financial advisors who claimed they understood federal benefits. <span class="pull">None of them actually did.</span> FedWise was the first place that explained my FERS Supplement in a way that made sense in five minutes.&rdquo;
      </blockquote>
      <div class="att">
        <div class="avatar">M</div>
        <div class="who">
          <strong>Maria, IRS Ogden</strong>
          <span>27 years of federal service</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ============== FINAL CTA ============== -->
  <section class="fw-final">
    <div class="fw-wrap">
      <div class="fw-final-inner">
        <h2>Stop guessing about <em>your</em> federal benefits.</h2>
        <p>The walkthrough takes about 15 minutes and you&rsquo;ll come out with a real understanding of your situation &mdash; and a real expert ready to answer the questions that came up along the way.</p>
        <div class="ctas">
          <a href="/fedwise/walkthrough/" class="fw-btn fw-btn-primary">Start your walkthrough <span class="fw-arrow">→</span></a>
          <a href="#" class="fw-btn fw-btn-ghost" onclick="document.getElementById('fwChatLauncher').click(); return false;">Just ask a question</a>
        </div>
      </div>
    </div>
  </section>

</div>
