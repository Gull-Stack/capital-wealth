# Capital Wealth — Project Context

## About
Capital Wealth is a registered investment advisor in Utah doing $100M/year in revenue. We serve federal employees, retirees, and high-net-worth individuals with retirement planning, tax strategy, and wealth management.

## Brand
- **Colors:** Navy (#0f2742), Gold (#fdd25e), White, Light Gray (#f8fafc)
- **Fonts:** Arial/Helvetica (email), Proxima Nova (web)
- **Tone:** Professional but approachable. Direct, not salesy. Educational, not pushy. We talk like real people, not robots.
- **Logo:** White horizontal at `assets/images/logos/logo-horizontal-white.png`
- **Address:** 1850 W Ashton Blvd Suite 175, Lehi, UT 84043
- **Phone:** 801.210.2800
- **VIP Email:** vip@capitalwealth.com

## Compliance (MUST follow)
- Every client-facing email and page MUST include: "Advisory services offered through Capital Wealth, LLC, a State of Utah Registered Investment Advisor. Insurance services offered through CWA Insurance Services, LLC."
- Federal content MUST include: "Capital Wealth is not affiliated with or endorsed by the U.S. Government, Social Security Administration, Office of Personnel Management, or any federal agency."
- Never guarantee returns. Never promise specific outcomes. Use "may", "can", "designed to help" language.
- All emails must have an unsubscribe link and physical mailing address.

## Tech Stack
- **Site:** Eleventy (11ty) static site, deployed to Vercel via GitHub push to main
- **CRM:** Salesforce (org alias: `cw`, authenticated as jcohen@capitalwealth.com)
- **Task Management:** Asana (workspace: capitalwealth.com)
- **Communication:** Zoom (no Slack — never suggest Slack)
- **Email:** Microsoft 365 (not yet authenticated for CLI)
- **Marketing Automation:** Salesforce Apex batch jobs for email campaigns
- **Images:** Hosted at capitalwealth.com/assets/images/brand/

## Team
- Mike Stevens — CEO / Lead Advisor
- Josh Cohen — Director of Marketing
- Chad Austin — Lead Advisor (handles lead confirmation calls)
- Brent Thompson — Relationship Advisor
- Sam Rios-Lazo — Wealth Associate
- Lateesha Stevens — Operations Manager
- Kayden Hicken — Operations / Admin
- Teresa Phillips — Admin / Client Services
- Bryce Morgan — Admin / Marketing Support
- Ann Werts — Federal benefits expert (external, 25+ years)

## Salesforce Fields to Know
- **Lead Source:** Where the lead came from (Website Form, WFR April 2026, Federal Workshop, etc.)
- **Attendance:** Booked → Confirmed → Attended → Followed Up After
- **Email Opens:** Auto-tracked count of email opens
- **Conversations:** Auto-calculated count of two-way interactions (answered calls, email replies, text replies)
- **Activity Disposition:** REQUIRED on all calls/texts. Pick from: Answered, Left Voicemail, No Answer, Busy, Wrong Number, Disconnected, Scheduled Appointment, Confirmed Attendance, Not Interested, Requested Callback, Text Sent, Text Reply Received, Email Sent, Email Reply Received

## Email Template Standards
- Max width: 680px
- Force light mode: `<meta name="color-scheme" content="light only">`
- Use `<meta charset="UTF-8">` in head
- All special characters as HTML entities (no raw unicode dashes/quotes)
- CTA buttons: #fdd25e background, #0f2742 text, uppercase, bold
- Footer must include: compliance disclaimer, physical address, unsubscribe link
- Images hosted on capitalwealth.com (never Google Drive)
- Send from vip@capitalwealth.com via Salesforce OrgWideEmailAddress

## Git Workflow
- Push to `main` triggers Vercel deploy automatically
- Create feature branches for large changes, PR for review
- Small fixes can go direct to main
- Commit messages: short, descriptive, no emoji

## Session Log

### 2026-05-14 — Moved `/l/federal-phase-one/` → `/start/`

- **What shipped:** Page that lived at `/l/federal-phase-one/` (30-min federal benefits scheduler — Zoom embed above the fold, team strip, value props, "what we'll cover") now lives at the shorter, brandable `/start/`. Source file moved [src/l/federal-phase-one/index.njk](../src/l/federal-phase-one/index.njk) → [src/start/index.njk](../src/start/index.njk) with one frontmatter line changed: `permalink: "/start/"`. All page content (premium brand palette work from `d60ccdeb`, `4ff89526`, `f43a88ce`, `6eedbad2` earlier this week) carried over untouched.
- **301 redirect added** in [vercel.json](../vercel.json): `/l/federal-phase-one` and `/l/federal-phase-one/` → `/start/` with `permanent: true`. Paired-entry style matches the SDBA dead-URL cleanup from commit `aa9111ff`. Old URL still works for any FB ads, email links, or external references.
- **Preserved deliberately:** `noindex: true` + `excludeFromSitemap: true` stayed on. The page is a scheduler landing target (Zoom iframe + value props), not an SEO target — same posture as before. Confirmed `/start/` does NOT appear in `_site/sitemap.xml`. Title still says "Book a 30-Minute Federal Benefits Call | Capital Wealth" since the audience is still federal traffic from ads + email.
- **Repo cleanliness:** Recent commits in this repo include both `src/` and `_site/` outputs (e.g. `d60ccdeb` shipped `_site/l/federal-phase-one/index.html` alongside the source). Matched that pattern: built `_site/start/index.html` is staged, stale `_site/l/federal-phase-one/index.html` is removed. Reverted an unrelated `_site/sitemap.xml` reorder that the local rebuild surfaced (alphabetical reshuffle, no real content change).
- **No other repo refs to update:** grep across `src/`, `api/`, `scripts/`, `*.json` returned only the source file itself. Nothing in nav, layouts, sitemap, robots, FB ad scripts, or email templates points at `/l/federal-phase-one/`.
- **What to pick up next:** (1) Open PR for this change. (2) After Vercel deploy: `curl -sI https://www.capitalwealth.com/start/` should return 200; `curl -sI https://www.capitalwealth.com/l/federal-phase-one/` should return 308 with `Location: /start/`. (3) Sanity-click a Zoom slot end-to-end on the live `/start/` page to confirm the embed still books. (4) Consider whether any active FB ad creatives need their final URL repointed from `/l/federal-phase-one/` to `/start/` — redirect handles it, but a direct link is cleaner for ad-system attribution. (5) If `/start/` is going to start showing up in non-federal contexts, consider flipping `noindex: true` → `false` and tweaking the title from "Federal Benefits Call" to something more general.

### 2026-05-12 (Late) — Marketing Hub Lightning App Page: Looker Studio GA4 embedded inside Salesforce

- **One-pager Mike opens:** [https://capitalwealth.lightning.force.com/lightning/n/CW_Marketing_Hub](https://capitalwealth.lightning.force.com/lightning/n/CW_Marketing_Hub). GA4 brand-awareness embed on top (via Looker Studio iframe), gold "Open Dashboard" button to the existing CW — Marketing Live dashboard below.
- **Components shipped via SF CLI metadata API** (committed under [salesforce/force-app/main/default/](../salesforce/)): `CspTrustedSite/Looker_Studio` (whitelists `lookerstudio.google.com` for iframes), `ApexPage/CW_Brand_Awareness_Embed` (Visualforce shell iframing the published embed URL), `FlexiPage/CW_Marketing_Hub` (Lightning App Page, default-app-home template, single "main" region), `CustomTab/CW_Marketing_Hub` (icon `Custom20: Heart`, surfaces in App Launcher).
- **Looker Studio report:** [f08fd886-91f2-4c0f-a28b-179a3dba0dfb](https://lookerstudio.google.com/reporting/f08fd886-91f2-4c0f-a28b-179a3dba0dfb). GA4 property `375072851`. Bryce changed share setting + needs to finish: (1) File → Embed report → toggle ON (so the iframe renders data), (2) Add 3 charts — Scorecard for Sessions, Table for Page paths, Donut for Session source — per [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md#looker-studio-setup), (3) Share with `mstevens@capitalwealth.com` as Viewer.
- **Metadata gotchas this session (saving these for next time):** `CustomTab` icon element is `<motif>` not `<icon>` (deploy error "Invalid motif:null" was misleading); `<mobileReady>` not valid in CustomTab v66; `flexipage:dashboard` component fails deploy on AppPage type FlexiPages in this org with "couldn't retrieve design time component information" — so embedding a SF dashboard directly inside an App Page isn't possible here (worked around with a gold-button link to the dashboard URL).
- **No app navigation edit performed** — intentionally avoided modifying `CustomApplication/Capital_Wealth` since Mike has his own customized `Capital_Wealth_Mike` app with a tailored homepage and roundtripping the production app metadata is risky. Marketing Hub tab is accessible via App Launcher; each user can pin it to nav via the per-user pencil → "Edit Capital Wealth navigation items" (4 clicks), or admin can do it globally via Setup → App Manager → Capital Wealth → Edit → Navigation Items.
- **What to pick up next:** (1) Bryce finishes the 3 charts in Looker Studio + enables embed + shares with Mike. (2) Mike pins "Marketing Hub" to his nav bar via the pencil icon at the right edge of the nav. (3) Merge [PR #29](https://github.com/Gull-Stack/capital-wealth/pull/29) (contains all the SFDX metadata for the dashboard + reports + Marketing Hub). (4) (Carried forward) Watch the Federal Workshops Educational FB campaign CPL at the ~36 hour mark — if under ~$15, pause old SAC campaign `120241691882900665`. (5) (Carried forward) Send the 48 SendGrid backfill emails. (6) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (7) (Carried forward) Rotate Zoom + FB secrets sent in plaintext earlier.

### 2026-05-12 — Marketing dashboard fully deployed: 6 reports + dashboard live in SF via metadata API, backfill applied

- **Built and deployed entirely via Salesforce CLI metadata API** (not the Lightning UI): 6 reports (R1–R6) in `4 - Marketing Reports` folder + `CW — Marketing Live` dashboard (Id `01ZVS000002sdBJ2AY`) in `4 - Marketing` folder. Source XML committed at [salesforce/force-app/main/default/](../salesforce/). Dashboard runs as Josh (full-org visibility), folder is shared with All Internal Users by default so Mike already has view access — no extra grant needed. Dashboard URL: https://capitalwealth.lightning.force.com/lightning/r/Dashboard/01ZVS000002sdBJ2AY/view
- **Backfill applied successfully** via [scripts/sf-backfill-campaigns.js](../scripts/sf-backfill-campaigns.js): 258 Lead `Campaign__c` updates + 56 new CampaignMember rows (202 already existed from May 11 webinar backfill). 7 transient Hill AFB failures during the batch run, all retried successfully. Final lead counts in last 30 days: **Webinar 296, Ogden 11, Hill AFB 9, SLC 0, General 3.** SLC=0 is expected — no traffic to that page yet because the May 20 event date is the latest of the three workshops.
- **PR #28 merged + deployed** before the backfill ran. Confirmed live by curl'ing the Ogden landing page and verifying the `<input type="hidden" name="campaign_id" value="701VS00000eWgMsYAK">` is in the rendered HTML.
- **Dashboard component-type gotchas hit while deploying** (so the next session knows): SF metadata enum names for `DashboardComponentType` are NOT what they're called in the Lightning UI — `HorizontalBar` → must be `Bar`; `VerticalColumnStacked` → must be `ColumnStacked` (and it requires `autoselectColumnsFromReport=true` OR no `secondaryGroupingColumn`); `RichText` is NOT a valid metadata enum at all (must be added via Lightning UI); `Table` components require `indicatorLowColor`/`indicatorMiddleColor`/`indicatorHighColor` even though they're never displayed; `titleSize` only accepts {8, 9, 10, 12, 14, 18, 24, 36}; `sortColumn` is only valid in Tabular format reports (not Summary). Report metadata also can't reference grouping fields as columns AND group on them — choose one.
- **What I dropped from the original plan vs. what I shipped:** (a) skipped per-channel KPI metric cards — replaced with the simpler "Today by Channel" bar chart that conveys the same info. (b) Dropped inline `<chart>` blocks on R1-R6 reports because charts are rendered by the dashboard component, not the report. (c) Dropped the rich-text "Looker Studio link" component because it's not a valid metadata enum — to be added manually in the SF UI after the GA4 dashboard is built.
- **Looker Studio next step (Bryce, ~5 min):** click the Linking-API URL in [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md), approve GA4 access, add 3 charts (scorecard for Sessions, table for Page path, donut for Session source). GA4 property ID `375072851`. Share with `mstevens@capitalwealth.com`. Copy the URL back into the SF dashboard as a Rich Text component (instructions in the doc).
- **What to pick up next:** (1) Build the Looker Studio dashboard (5-min task). (2) Add the Rich Text component with the Looker URL to the top of the SF dashboard via Lightning UI. (3) Have Mike open the dashboard and confirm it answers what he wanted. (4) Watch the Federal Workshops educational FB campaign CPL at the ~36 hour mark — if under ~$15, pause the old SAC campaign `120241691882900665`. (5) (Carried forward) Send the 48 SendGrid backfill emails. (6) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (7) (Carried forward) Rotate Zoom + FB secrets sent in plaintext earlier.

### 2026-05-11 (Late PM) — Marketing dashboard foundation: wired federal-event pages to their SF Campaigns + built dashboard build guide

- **Why this came up:** Josh built a sales-funnel campaigns dashboard (Leads → Attended → Blue Sheet → Converted → Production). Mike asked for a separate **marketing** view: brand-awareness (GA4) + live lead counts split into 5 buckets — Webinar 5/14, Workshop Ogden 5/19, Workshop SLC 5/20, Workshop Hill AFB 5/21, and General Contact.
- **The blocker I found while planning:** today, all 4 federal-event landing pages AND the general contact form submit leads that land in the **same default Campaign** (`Website Form 2026` = `701VS00000dB91aYAC`) with `LeadSource='Website Form'` (or `'Webinar'` for webinar). The 5 buckets are **indistinguishable in structured SF data** — the only difference is `Description LIKE '%Event Date: 2026-05-19%'`, and **Description is a long-text area that cannot be filtered in SOQL** (`INVALID_FIELD: field 'Description' can not be filtered`). Without fixing this, no per-event report is possible.
- **What shipped in [PR #28](https://github.com/Gull-Stack/capital-wealth/pull/28):** (a) Added a hardcoded `<input type="hidden" name="campaign_id" value="…">` to each of the 4 event landing pages so [api/submit-lead.js:186](../api/submit-lead.js:186) (which already validates+uses `campaign_id`) routes new leads to the right SF Campaign on creation. Webinar → `701VS00000eWrYVYA0`, Ogden → `701VS00000eWgMsYAK`, SLC → `701VS00000eWe58YAC`, Hill AFB → `701VS00000eWqE8YAK`. (b) Patched [api/submit-lead.js](../api/submit-lead.js) to auto-set `Are_You_Federal__c='Yes'` on both insert AND existing-lead update when `isFederalLead` is true — previously this only got set via manual SF bulk updates, which left the Federal vs General segmentation broken for new submissions. (c) Added [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md) — click-by-click checklist for the 6 reports + 1 SF dashboard + 1 Looker Studio dashboard, including exact filter values, Campaign IDs, the navy/gold color hint, and Looker Studio widget specs.
- **One-time backfill staged but NOT run:** `tmp/backfill-campaigns.js` (gitignored). Dry-run found **268 leads** currently sitting in the default catch-all over the last 30 days: 245 Webinar (need Campaign__c flip — CampaignMember rows already exist from the May 11 backfill), 11 Ogden, 0 SLC, 9 Hill AFB, 3 unmatched (general contact, correctly stays in catch-all). Script also sets `Are_You_Federal__c='Yes'` on all 265 matched. Blocked on PR #28 merge + deploy — running before that would attach historical leads to campaigns whose form pages still send everything to the default, creating an inconsistent before/after state.
- **Why click-checklist instead of metadata XML for the SF dashboard:** SF Report metadata is verbose and brittle (each report = ~50 lines of nested XML for columns/groupings/filters/charts), this repo has no SFDX project set up, and Josh has already built one dashboard manually in ~hours so a checklist hand-off matches his workflow. Checklist puts Mike in front of working numbers in 60–90 min of Josh's time post-merge.
- **GA approach locked in:** Looker Studio dashboard (free, GA4-native) linked from a Rich Text component at the top of the SF dashboard. Not building a GA→SF custom object pipeline yet — half a day of cron+auth+schema work for a V1 that may or may not get used. If Mike loves the link-out, upgrade later.
- **What to pick up next:** (1) Review + merge [PR #28](https://github.com/Gull-Stack/capital-wealth/pull/28). (2) After Vercel deploys: `node tmp/backfill-campaigns.js --dry-run`, confirm counts, then `node tmp/backfill-campaigns.js` (no flag) to apply. (3) Josh follows [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md) — builds 6 reports in `4 - Marketing Reports` folder, assembles `CW — Marketing Live` dashboard with Running User=`jcohen@capitalwealth.com`. (4) Build the Looker Studio dashboard per the same doc, share with `mstevens@capitalwealth.com`, paste URL into the Rich Text component. (5) Submit 4 test leads (one from each event page) with email pattern `brycedmorgan+marketing-test-<bucket>@gmail.com` and verify with the SOQL query at the bottom of the doc. (6) Watch the existing FB Workshops educational campaign CPL for the **~36 hour** mark (carried forward from earlier session) — if under ~$15 CPL, pause the old SAC campaign `120241691882900665`. (7) (Carried forward) Send the 48 SendGrid backfill emails. (8) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (9) (Carried forward) Rotate Zoom + Facebook secrets sent in plaintext earlier.

### 2026-05-11 (PM) — Federal Workshops ad rebuild: killed SAC cap on targeting, replaced with educational LAL clone of the Webinar pattern

- **Root cause of "the Workshops ads suck":** Campaign `120241691882900665` ("CW — Federal Workshops — May 19-21 — Leads") was flagged with `special_ad_categories = ['FINANCIAL_PRODUCTS_SERVICES']`, which strips detailed targeting / employer targeting / sub-1% LAL / narrow age windows. Running at **$29.18 CPL** for 30d. Compare: the Webinar campaign (`120241615030540665`) with `special_ad_categories = []` is running at **$10.80 CPL** off the same Fed Employees Source List using a 1% LAL — 2.7x cheaper leads. The old "Federal Seminar Event" the user remembered as "successful" was actually the worst performer in account history at $166.28 CPL.
- **What shipped (live on FB account `act_956377226671159` as of this session):** New campaign `120241962930110665` "CW — Federal Workshops — Educational (May 19-21)", no SAC, OUTCOME_LEADS, OFFSITE_CONVERSIONS optimization, pixel LEAD event. Three ad sets, each $350/day, LAL 1% of `Fed Employees - Source List` (audience id `120241513320320665`) with `Fed Employees - Source List` + CRM List excluded, age 25-65, Advantage+ placements: AS1 Ogden (id `120241962936030665`, city key 2532136 ±25mi, ends Tue May 19 1pm), AS2 SLC (id `120241963276230665`, city key 2532348 ±19mi, ends Wed May 20 1pm), AS3 Hill AFB/Layton (id `120241963280110665`, city key 2531858 ±15mi, ends Thu May 21 3pm). Nine ads total (3 angles × 3 locations): Authority/Five-Systems/Decisions. Reused proven Webinar image hash `e09210ed50e9005befa6f405790303b4`. UTMs: `utm_campaign=fed-workshops-edu-may2026&utm_content=<slug>-<angle>`. Old SAC campaign still ACTIVE intentionally — see pickup #1 below.
- **Copy softening per non-promissory RIA language standard ([PR #23](https://github.com/Gull-Stack/capital-wealth/pull/23), merged):** H1 on all 3 workshop landing pages changed from "Two hours that **change** thirty years…" → "Two hours that **could shape** thirty years…". All 9 ad bodies use "could shape", "can shape", "may", never "will". Same PR added `/privacy/` ([src/privacy/index.njk](../src/privacy/index.njk)) — was 404 site-wide despite being linked from calculator, SDBA, and other pages' footers. Created to satisfy Meta App publish requirement AND fix the broken site-wide link.
- **FB App + System User permission gotchas hit (and resolved) on the way:** (a) "Capital Wealth Ads" FB App (id `831374225889248`) was in Development mode — couldn't attach API-built creatives to ads, error "Ads creative post was created by an app that is in development mode." Fix: user flipped App Mode to Live in `developers.facebook.com/apps/831374225889248/settings/basic/` after filling Privacy Policy URL (the now-live `/privacy/`), App Icon, Category, Contact email. (b) System User "Melvin" (id `122111746130947675`, the owner of `FB_ACCESS_TOKEN`) had zero pages assigned — error "Missing Page permission… contact admin for Advertiser role or higher." Fix: user went to `business.facebook.com/settings/system-users` → Melvin → Add Assets → Pages → Capital Wealth (id `1472234546327675`) → toggled on "Manage Page" → Save. (c) Deprecated `degrees_of_freedom_spec.creative_features_spec.standard_enhancements.enroll_status` field — removed from creative spec. (d) `is_adset_budget_sharing_enabled=false` is now required at campaign creation when not using campaign budget.
- **Tooling (in [PR #25](https://github.com/Gull-Stack/capital-wealth/pull/25)):** Six zero-dependency Node scripts under `scripts/fb-*.js` with inline `.env` loader. `fb-diagnose.js` (multi-window insights + auto findings — dead ads, fatigue, low CTR, CPL trend decay, demographic leaks). `fb-compare-events.js` (lifetime Seminar vs Workshop vs Webinar with SAC status). `fb-build-workshops-edu.js` (idempotent builder — reuses campaign/adsets/ads by name on re-run; supports `--dry-run` and `--campaign-id <id>`). `fb-launch.js` (pre-flight ad review check then flips ACTIVE). `fb-pull-rebuild-inputs.js` / `fb-pull-city-keys.js` (read-only discovery). `tmp/` gitignored.
- **What to pick up next:** (1) **~48 hours from now** (around 2026-05-13 PM), check the new campaign's CPL — if it's tracking under ~$15 (i.e., Webinar-like rather than SAC-like), **pause** the old SAC campaign `120241691882900665` to redirect spend. Easiest: `node scripts/fb-diagnose.js`, then a single API call to set `status=PAUSED`. (2) **Review the privacy policy text** at [src/privacy/index.njk](../src/privacy/index.njk) for accuracy with actual data practices — I drafted it conservatively but a real compliance review is warranted. (3) (Carried forward) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py`). (4) (Carried forward) Rotate Zoom + Facebook secrets that were sent in plaintext during earlier sessions. (5) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist.

### 2026-05-11 — utahfed.com / idahofed.com billboard hub → federal-workshops chooser page

- **What shipped (this PR on branch `claude/flamboyant-ishizaka-55885b`):** New hub landing page at [src/l/federal-workshops/index.njk](../src/l/federal-workshops/index.njk) that lists the three May 2026 federal workshops as clickable cards (Ogden 5/19, SLC 5/20, Hill AFB 5/21). Each card CTA points to its existing per-workshop landing page with `?lead_source=Billboard` appended. The three existing workshop pages ([src/l/federal-benefits-workshop-{ogden,slc,hill-afb}/index.njk](../src/l/)) gained a `<input type="hidden" name="lead_source" id="lead_source">` near line 944 and the inline submit handler's query-param copier now includes `'lead_source'` alongside the UTM keys, so the value flows through to `/api/submit-lead`. [api/submit-lead.js:146](../api/submit-lead.js:146) `ALLOWED_LEAD_SOURCES` set now includes `'Billboard'`. [vercel.json:24](../vercel.json) gained four host-based redirects (utahfed.com, www.utahfed.com, idahofed.com, www.idahofed.com → `https://www.capitalwealth.com/l/federal-workshops/?lead_source=Billboard`) modeled on the existing `retireutah.com` pattern.
- **Why the redirect carries `?lead_source=Billboard` itself:** Defense-in-depth. If a billboard visitor lands on the hub and shares the link with a friend, that friend's session may not have the query string when they click a workshop card — but the hub→workshop link bakes `?lead_source=Billboard` into the href, so attribution still survives the second hop.
- **Salesforce picklist note:** `Lead.LeadSource` is **not a restricted picklist** (`restrictedPicklist: false` per the SObject describe), so SF will happily accept `"Billboard"` even though it's not currently in the picklist values — leads will save fine the moment code deploys. **However**, until Josh adds `Billboard` to the picklist via SF Setup → Lead → Fields → LeadSource, it won't appear as a filter option in reports/dashboards. This is a 30-second admin task; not a blocker for shipping.
- **DNS / Vercel domains dependency (NOT a code task):** The four redirect rules only fire once `utahfed.com`, `www.utahfed.com`, `idahofed.com`, `www.idahofed.com` are attached to the Vercel project (Settings → Domains) AND DNS at the registrar points to Vercel (typically A record `76.76.21.21` for apex, CNAME `cname.vercel-dns.com` for www). Confirm with whoever holds the registrar logins for these two domains before announcing the billboards. Until then, the hub page at `/l/federal-workshops/` still works on capitalwealth.com directly.
- **Verification after merge → main → Vercel deploy:** (1) Visit `https://www.capitalwealth.com/l/federal-workshops/` — three cards render in date order, navy/gold + federal palette, compliance footer present. (2) Click any card → URL on destination should contain `?lead_source=Billboard`; complete the form with a test address like `brycedmorgan+billboard-test@gmail.com`; then `sf data query -o cw -q "SELECT Id, Email, LeadSource FROM Lead WHERE Email='brycedmorgan+billboard-test@gmail.com' ORDER BY CreatedDate DESC LIMIT 1"` — expect `LeadSource = 'Billboard'`. (3) Negative case: submit a fresh lead from a workshop page **without** the query string — `LeadSource` should fall back to `'Website Form'`. (4) Once DNS is live: `curl -sI https://utahfed.com/` and `curl -sI https://idahofed.com/` should both 308 to the hub URL with the query string.
- **What to pick up next:** (1) Open the PR for this branch and ship. (2) Confirm the DNS / Vercel domain attachment for both apex domains. (3) Have Josh add `Billboard` to the SF Lead.LeadSource picklist so it shows up cleanly in report filters. (4) (Carried forward) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py`). (5) (Carried forward) Rotate Zoom + Facebook secrets that were sent in plaintext during the previous session.

---

### 2026-05-11 — Federal Webinar registration pipeline: env fix, SF patch, 48-lead backfill, broken phone validation hotfix

- **Root cause of "Zoom Events count looks wrong" concern:** The Zoom Events count (161/165) was actually correct — the Webinar+ migration ported registrations cleanly. The real issue was that the website form's Zoom integration ([api/submit-lead.js](../api/submit-lead.js)) had been silently no-op'ing since it shipped May 5: all four `ZOOM_*` env vars in Vercel production were empty strings. Every website-form submission since May 5 created an SF Lead but no Zoom registrant.
- **Env vars set in Vercel production:** `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, `ZOOM_WEBINAR_ID=82502463485` (underlying webinar inside the migrated Zoom Events session — same numeric ID still works with legacy `/v2/webinars/{id}/registrants` API). Plus `FB_ACCESS_TOKEN` + `FB_AD_ACCOUNT_ID=956377226671159` for Facebook Ads spend pulls via [api/facebook-ads.js](../api/facebook-ads.js). Preview left empty intentionally.
- **Two real bugs patched in [PR #21](https://github.com/Gull-Stack/capital-wealth/pull/21):** (1) SF Lead update path was 400-ing because a validation rule now requires `How_did_you_hear_about_us__c` on update — patched to backfill `'Website'` when blank. (2) TY page `.ty-action-btn` CSS overrode the `[hidden]` HTML attribute, so the "Save Zoom Join Link" button rendered with `href="#"` even when no join URL was available — patched with `[hidden] !important` reset.
- **Urgent hotfix [PR #22](https://github.com/Gull-Stack/capital-wealth/pull/22) — phone validation broken site-wide:** [PR #20](https://github.com/Gull-Stack/capital-wealth/pull/20) moved phone validation into the shared `window.CWPhone` helper in [src/assets/js/main.js](../src/assets/js/main.js) but `base-finpay.njk` (the federal landing page layout) never loaded `main.js`. Result: `window.CWPhone` was `undefined`, every form submission rejected with "Please enter a valid US phone number." Fix: add `<script src="/assets/js/main.js">` before `</body>` in [src/_includes/layouts/base-finpay.njk](../src/_includes/layouts/base-finpay.njk).
- **Backfill completed:** 48 affected website-form leads (May 5–11 window) registered with Zoom via single-registrant API. Each SF Lead Description appended with `Zoom Registrant ID` + `Zoom Join URL`. Confirmation emails suppressed via webinar setting (`registrants_confirmation_email: false`) — Zoom Events' built-in reminder system will deliver join links pre-event. SendGrid follow-up email TO THE 48 still pending user OK.
- **All 253 webinar leads (LeadSource='Webinar' since May 5) marked `Are_You_Federal__c = 'Yes'`** via SF bulk update. Per Josh: "These are all federal leads."
- **SF Campaign work:** Added 203 missing Leads as CampaignMembers to `701VS00000eWrYVYA0` (Federal Benefits Workshop 5.14.26) — Campaign now has all 253. Created Salesforce Report **`Federal Webinar 5.14.26 — Members & $`** (Id `00OVS000008BqDB2A0`) showing registrants + Actual Cost. Set `Campaign.ActualCost = $5,153.37` from FB campaign `CW — Federal Webinar — May 14 — Leads` (id `120241615030540665`).
- **What to pick up next:** (1) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py` — needs user "send" confirmation). (2) Delete 3 synthetic Zoom registrants left from diagnosis: `claude-diag-probe-*@example.com`, `claude-probe2-*@example.com`, `brycedmorgan+webprobe1778514614@gmail.com` — easiest in the Zoom Events admin UI under registrant list. (3) **Rotate the Zoom Server-to-Server OAuth client secret** since it was sent in plaintext chat. (4) Rotate the Facebook system-user access token (also sent in plaintext). (5) Optionally wire a Vercel cron to refresh Campaign.ActualCost daily from Facebook spend.
- **Reference:** Zoom webinar host email is `bmorgan@capitalwealth.com` — Bryce can't self-test the registration form with his own email (Zoom rejects host as registrant). Webinar's `registrants_confirmation_email` is OFF — Zoom doesn't email registrants automatically; rely on Zoom Events reminders or our own SendGrid.
