# Capital Wealth — Project Context

## About
Capital Wealth is a registered investment advisor in Utah doing $100M/year in revenue. We serve federal employees, retirees, and high-net-worth individuals with retirement planning, tax strategy, and wealth management.

## Brand
- **Colors:** Navy (#0f2742), Gold (#fdd25e), White, Light Gray (#f8fafc)
- **Fonts:** Arial/Helvetica (email), Proxima Nova (web)
- **Tone:** Professional but approachable. Direct, not salesy. Educational, not pushy. We talk like real people, not robots.
- **Logo:** White horizontal at `assets/images/logos/logo-horizontal-white.png`
- **Address:** 1850 W Ashton Blvd Suite 175, Lehi, UT 84043
- **Phone:** 801.210.2800
- **VIP Email:** vip@capitalwealth.com

## Compliance (MUST follow)
- Every client-facing email and page MUST include: "Advisory services offered through Capital Wealth, LLC, a State of Utah Registered Investment Advisor. Insurance services offered through CWA Insurance Services, LLC."
- Federal content MUST include: "Capital Wealth is not affiliated with or endorsed by the U.S. Government, Social Security Administration, Office of Personnel Management, or any federal agency."
- Never guarantee returns. Never promise specific outcomes. Use "may", "can", "designed to help" language.
- All emails must have an unsubscribe link and physical mailing address.

## Tech Stack
- **Site:** Eleventy (11ty) static site, deployed to Vercel via GitHub push to main
- **CRM:** Salesforce (org alias: `cw`, authenticated as jcohen@capitalwealth.com)
- **Task Management:** Asana (workspace: capitalwealth.com)
- **Communication:** Zoom (no Slack — never suggest Slack)
- **Email:** Microsoft 365 (not yet authenticated for CLI)
- **Marketing Automation:** Salesforce Apex batch jobs for email campaigns
- **Images:** Hosted at capitalwealth.com/assets/images/brand/

## Team
- Mike Stevens — CEO / Lead Advisor
- Josh Cohen — Director of Marketing
- Chad Austin — Lead Advisor (handles lead confirmation calls)
- Brent Thompson — Relationship Advisor
- Sam Rios-Lazo — Wealth Associate
- Lateesha Stevens — Operations Manager
- Kayden Hicken — Operations / Admin
- Teresa Phillips — Admin / Client Services
- Bryce Morgan — Admin / Marketing Support
- Ann Werts — Federal benefits expert (external, 25+ years)

## Salesforce Fields to Know
- **Lead Source:** Where the lead came from (Website Form, WFR April 2026, Federal Workshop, etc.)
- **Attendance:** Booked → Confirmed → Attended → Followed Up After
- **Email Opens:** Auto-tracked count of email opens
- **Conversations:** Auto-calculated count of two-way interactions (answered calls, email replies, text replies)
- **Activity Disposition:** REQUIRED on all calls/texts. Pick from: Answered, Left Voicemail, No Answer, Busy, Wrong Number, Disconnected, Scheduled Appointment, Confirmed Attendance, Not Interested, Requested Callback, Text Sent, Text Reply Received, Email Sent, Email Reply Received

## Email Template Standards
- Max width: 680px
- Force light mode: `<meta name="color-scheme" content="light only">`
- Use `<meta charset="UTF-8">` in head
- All special characters as HTML entities (no raw unicode dashes/quotes)
- CTA buttons: #fdd25e background, #0f2742 text, uppercase, bold
- Footer must include: compliance disclaimer, physical address, unsubscribe link
- Images hosted on capitalwealth.com (never Google Drive)
- Send from vip@capitalwealth.com via Salesforce OrgWideEmailAddress

## Git Workflow
- Push to `main` triggers Vercel deploy automatically
- Create feature branches for large changes, PR for review
- Small fixes can go direct to main
- Commit messages: short, descriptive, no emoji

## Session Log

### 2026-05-22 — SDBA indexing diagnosis + internal-link boost for `/services/sdba/`

- **Why:** Bryce asked why Google still won't index `https://www.capitalwealth.com/services/sdba/`. GSC URL Inspection screenshot showed **"Discovered – currently not indexed"** with **Last crawl: N/A** — i.e. Google knows the URL (via sitemap + a footer link on `/terms/`) but has **never fetched it**. So this is NOT a content/canonical/noindex problem (verified: live 200, no `X-Robots-Tag`, no `noindex`, self-referential canonical, in sitemap, robots.txt open, ~10K words). It's a **crawl-priority / authority** issue. Tell-tale: the only referring page GSC cited was `/terms/` (boilerplate footer link), which Google heavily discounts.
- **What shipped (uncommitted on this branch — 4 source files + their built `_site/` outputs):** added strong *contextual, in-body* internal links to `/services/sdba/` from heavily-crawled, topically-relevant federal pages (which get ad-driven crawl traffic), since the page previously only had boilerplate nav/footer + a few links it wasn't getting crawl credit for:
  - [tsp-mutual-fund-window/index.njk](../src/services/federal-benefits/tsp-mutual-fund-window/index.njk): added "SDBA Management" as the **first** Related-Services grid card + an in-body line framing the Window as "the TSP's version of a self-directed brokerage account (SDBA)" (descriptive anchor). Most relevant page — a TSP MFW *is* an SDBA.
  - [tsp-planning/index.njk](../src/services/federal-benefits/tsp-planning/index.njk): in-body sentence in the "Six Core TSP Funds" section linking "self-directed brokerage account (SDBA)".
  - [federal-retirement-checklist/index.njk](../src/services/federal-benefits/federal-retirement-checklist/index.njk): added an "SDBA Management" resource card right after the TSP MFW card.
  - [sdba/faq/index.njk](../src/services/sdba/faq/index.njk): the sibling FAQ page was missing an up-link to the hub — added "SDBA management overview" link in its closing CTA so the cluster points up to `/services/sdba/`.
- **Already-existing contextual links (left as-is, confirmed good):** homepage tile ([index.njk:1347](../src/index.njk)), services hub ([services/index.njk:589](../src/services/index.njk)), the SDBA guide (3× up-links), and the `401k-brokerage-window` sub-page.
- **Verified:** `npx @11ty/eleventy` clean; all 4 new links present in rendered `_site/` HTML with correct descriptive anchor text. Reverted the usual harmless `_site/sitemap.xml` reorder (per prior-session convention).
- **What to pick up next:** (1) **In GSC, click `REQUEST INDEXING`** on `https://www.capitalwealth.com/services/sdba/` — single most effective action for "Discovered – not indexed"; re-request in ~2 wks if still unindexed. (2) Commit → PR → merge → Vercel so the new internal links go live (they're what raise crawl priority). (3) Optional/secondary: once Google *does* crawl it, watch for cannibalization across the 4 indexable SDBA URLs (hub / 401k-brokerage-window / faq / guide) — make `/services/sdba/` the unambiguous hub if Google picks a sibling as canonical. (4) Longer-term: site-wide authority/crawl-budget is the real constraint; more contextual links from indexed blog posts would compound.

- **Email list (DONE, in hand — no scraping needed):** Bryce had a spreadsheet `Federal Employees $100K+ — All 50 States + DC (2024).xlsx` (51 sheets, one per state) that already includes name + agency + location + **Estimated Email**. Parsed it with a built-in-Python xlsx reader (no openpyxl). **55,694 total $100K-tier feds; filtered to the 6 reachable RIF target agencies = 14,160 deliverable contacts** (USDA 10,403, DOT 1,737, DOE 1,038, GSA 800, HUD 106, SBA 76). **DOD/Army/Navy/Air Force are absent** from this dataset (names withheld from the public FOIA release) — would need a separate source. Output: `~/Downloads/fed-100k-RIF-agencies-2024.csv` (cols: first,last,middle,email,agency,state,salary,grade,occupation). **Utah = 3,085**; also CA/MD/TX/VA/CO/WA big.
- **Utah warm-up batches carved:** `~/Downloads/utah-webinar-batches/utah-batch-01..06.csv` (50 / 100 / 250 / 500 / 1000 / 1172), sorted cleanest-agency-first, + `utah-all.csv` (3,072). Built [scripts/derive-fed-emails.js](../scripts/derive-fed-emails.js) — name+agency → candidate emails with a confidence flag (GSA/USDA/SBA/HUD high; DOD/DOE low) — kept as the reusable engine even though the xlsx already had emails.
- **SendGrid deliverability isolation set up (DONE):** authenticated the dedicated sending subdomain **`email.capitalwealth.com`** in SendGrid (Cloudflare DNS — all 5 CNAMEs DNS-only/unproxied + the `_dmarc.email` TXT). Verified green after the DMARC record was added (diagnosed via `dig` on 1.1.1.1/8.8.8.8 — the "validate your links" error was just SendGrid cert/cache lag, not a DNS problem). Plan: **Basic 5k plan ($15), NOT Advanced** — at this bursty low volume a dedicated IP would *hurt* deliverability. Sender: From `webinars@email.capitalwealth.com`, Reply-To `vip@capitalwealth.com`. Warm-up: send batch 1 (50) → ramp daily.
- **Email made SendGrid-ready:** [emails/vera-vsip-rif-webinar-invite-2026-05-28.html](../emails/vera-vsip-rif-webinar-invite-2026-05-28.html) unsubscribe swapped to the `<%asm_group_unsubscribe_raw_url%>` token.
- **Lead-capture build (Bryce's "register / newsletter→SF / watch replay→SF" vision) — built, locally verified, UNCOMMITTED:**
  - **Newsletter signup** on `/webinars/` (First/Last/Email → "The Federal Benefit Gap") → posts `lead_type=newsletter-signup`, `lead_source=Newsletter`.
  - **FERS replay light gate** on `/blog/fers-pension-explained/`: a navy "Register to watch" card replaces the video; on submit (`lead_type=webinar-replay`, `lead_source=Webinar Replay`) it reveals the YouTube embed (deferred via `data-src`, so the video doesn't load pre-gate) and remembers returning visitors via `localStorage`. Per Bryce's choice (light gate over free+CTA).
  - **[api/submit-lead.js](../api/submit-lead.js):** added 2 new LeadSource values, an `isEmailOnly` exemption (no phone for newsletter/replay), federal-flagging for both, two compact compliant confirmation-email builders, and an **isolated early-return branch** for the two new lead_types so the existing webinar/workshop/contact paths are untouched (the endpoint also powers the live May 28 reg form — blast radius kept minimal). `node --check` passes.
- **What to pick up next:** (1) **Deploy** ([task #11]): commit → PR → merge → Vercel. (2) **Post-deploy tests (critical, shared endpoint):** existing webinar reg still works; newsletter → SF lead 'Newsletter' + welcome email; replay gate → SF lead 'Webinar Replay' + reveal + email. (3) Josh adds 'Newsletter' + 'Webinar Replay' to the `Lead.LeadSource` picklist (saves regardless; needed for report filters — same as Billboard). (4) **Then** send SendGrid batch 1 — NOT before `/webinars/` is live (the invite links to it). (5) Subject line: confirm Ann's exact "know your options" wording. (6) Still open: slide decks (have 5 of Ann's Keynotes: TSP 101/201/301, FEHB, SS+Medicare; still need VERA/FERS Pension/FERS Benefits/Retirement Process), VERA calculator, live chat, Vernal event, competitor research.

### 2026-05-21 (PM2) — Ann meeting → schedule reorder + email-only push for May 28; full task board

- **Reconciled the `/webinars/` series against the real Ann meeting** (the prior pass used the wrong transcript). Two concrete builds shipped (uncommitted):
  - **Schedule update** in [src/_data/webinars.js](../src/_data/webinars.js): **FEHB (Nov 12) + Medicare (Nov 26) moved to November** — Ann was explicit feds won't engage before Open Season (2nd Mon Nov → 2nd Mon Dec); she wants 2–3 webinars that month. Reordered the middle to Ann's build sequence: VERA/VSIP/RIF (May 28) → FERS Benefits (Jun 11) → Supplement & Social Security (Jun 25) → TSP 101/201/301 (Jul 9 / Jul 23 / Aug 6) → TSP Master/Roth (Aug 20) → FEGLI (Sep 3) → Retirement Process (Sep 17) → FEHB (Nov 12) → Medicare (Nov 26). **Sep 17 → Nov 12 leaves October open** (intentional pre-Open-Season lull — could slot a repeat or Survivor Benefits). Rebuilt + verified rendered order.
  - **Email-only invite** for May 28: new [emails/vera-vsip-rif-webinar-invite-2026-05-28.html](../emails/vera-vsip-rif-webinar-invite-2026-05-28.html). Mike killed FB ads for this one — email only. Ann's angle: educational, "**know your options**," no sales pitch. Mirrors the FERS-recap template (680px, light-lock, gold CTA, both compliance disclaimers, unsubscribe, address). Register CTA → `/l/federal-benefits-webinar/`; cross-promo → `/webinars/`. **Subject line is a placeholder — slot in Ann's exact "know your options" language she sent.**
- **Target audience for the email (RIF-affected agencies Ann named):** GSA, Dept of Energy, Dept of Transportation, USDA, Dept of Defense, SBA, HUD.
- **Created a 9-item task board** (TaskCreate) capturing everything from the meeting. Done: #1 schedule, #2 email. Open/blocked: #3 FedsDataCenter email-list builder (needs approach decision + deliverability/ToS review; fold in Ann's Utah $100K list she airdropped), #4 webinar slide decks (BLOCKED on Ann's source files + FedTools VERA guide), #5 VERA/VSIP eligibility calculator, #6 live chat 8–5 on federal pages (Ann prefers live chat over a bot; needs staffing decision), #7 newsletter legislative-updates section (source: "Fed Family" FB monthly bills), #8 FB/social pivot to federal + ProFeds/Workshops-for-Retirement seat-filling research, #9 Vernal June in-person event + agency-by-location outreach.
- **Key Ann strategy notes (for next session):** (a) near-term audience (retiring ≤5 yrs) engages *because* they DON'T want self-service — they want a human; defer the self-service doc-upload comparison tool. (b) Agency-sponsored classes pitch = "you pay us as a contractor for an educational class, no sales pitch" — FEHB is a strong door-opener (agencies dropped health fairs post-COVID; premiums spiking). (c) Eventually pre-record webinars + replay live with live Q&A. (d) Run events/email **by location**, not blast-everyone.
- **What to pick up next:** (1) Get Ann's slide source files (FERS Pension, the 2-hr session, TSP 101/201/301, TSP Master, FEGLI, Retirement Process, FEHB 2021–23 Open Season decks, SS+Medicare combined deck) + her FedTools VERA/VSIP guide — then build decks via Claude Design (model: /workshop), each ending with a "next webinar / upcoming schedule" slide (Mike wants it). (2) Get Ann's exact email subject/headline language. (3) Decide FedsDataCenter approach (scrape/paginate the bot vs. $1 first-month paid export) + deliverability plan (batch ~50, SendGrid). (4) Decide live-chat staffing. (5) Confirm Oct-gap handling + whether Survivor Benefits becomes a 13th topic. (6) None committed yet — open a PR when ready.

### 2026-05-21 (PM) — Built the Federal Webinar Series + /webinars/ hub (register upcoming + watch replays)

- **What shipped (uncommitted on this branch):** a data-driven webinar hub at **`/webinars/`** — "Next webinar" featured band + the full 12-session series grid + a "Watch past webinars" replay section + what-to-expect + FAQ + soft CTA to `/start/` + compliance block. Standard site chrome (uses `layouts/base.njk`), indexable, with `Event` JSON-LD for the next live session and `faqSchema` (6 Q&A).
- **Source of truth:** new [src/_data/webinars.js](../src/_data/webinars.js) — ordered array of 12 episodes, each with title/blurb/date/status. `status: "past" | "next" | "scheduled"` drives rendering (past → YouTube replay link, next → live Register button, scheduled → "Registration opens soon"). Model is **per-topic, built as you go** (per the 2026-05-21 leadership decision) — only May 28 is live; the other 10 show their date but aren't registerable until each Zoom is built.
- **The 12 topics** came straight from Ann's `Menu of Topics for 45-60 minute presentations 2026 (2).pages` (Bryce sent it). The `.pages` is Apple IWA binary — extracted the full text by decompressing `Index/Document.iwa` (Snappy framing) with a throwaway Python script (`/tmp/iwa_decode.py`), since `preview.jpg` only renders page 1. Topics: FERS Pension (done May 14), FERS Benefits, FERS Supplement & SS, VERA/VSIP/RIF (May 28), TSP 101/201/301, TSP Master Class (Roth Conversions), FEGLI, FEHB Best Health Plan, Medicare & FEHB, The Retirement Process.
- **Proposed schedule = every-other-Thursday** starting from the two fixed anchors (May 14 past, May 28 next): Jun 11, Jun 25, Jul 9, Jul 23, Aug 6, Aug 20, Sep 3, Sep 17, Oct 1, Oct 15. **These dates/order are a DRAFT** — Bryce + Ann confirm the running order (meeting action item). **Flag baked into the data file + page:** Ann's notes say FEHB (#10) is "typically conducted in November during Open Season," so FEHB + Medicare may want to move to November.
- **Two existing pages wired in as the first live entries:** May 28 VERA/VSIP/RIF → the evergreen registration page [/l/federal-benefits-webinar/](../src/l/federal-benefits-webinar/index.njk) (re-pointed each cycle), and May 14 FERS Pension → the existing YouTube replay at [/blog/fers-pension-explained/](../src/blog/fers-pension-explained/) (`Bd-eW4XrztA`).
- **Nav:** added "Federal Webinar Series" → `/webinars/` to the Federal Benefits menu in all three nav surfaces — [base.njk](../src/_includes/layouts/base.njk) (desktop mega + mobile drawer), [base-finpay.njk](../src/_includes/layouts/base-finpay.njk) (most pages; desktop + mobile), and [header-mm.njk](../src/_includes/components/header-mm.njk) (homepage). Link now renders on 180 built pages — hence the large `_site/` diff (shared-layout change ripples to every page).
- **Verified locally:** `npx @11ty/eleventy` clean; `/webinars/` renders 12 cards + 1 replay + 1 Event schema + compliance disclaimer; in-browser screenshot confirmed the hero ("Federal retirement, *decoded.*"), the May 14 REPLAY card, and the May 28 REGISTER NOW card. Fixed a real bug: the site's global `h1`/heading color was overriding the white hero text (set explicit colors on all hub headings, light + dark backgrounds). Added [.claude/launch.json](launch.json) (eleventy `--serve` on :8099) for the preview tool.
- **Why a screenshot dance:** the eleventy `--serve` live-reload kept snapping the preview tab back to `/`; got clean shots by `location.replace('/webinars/')` immediately followed by the screenshot call.
- **What to pick up next:** (1) Bryce confirms the topic **order + dates** with Ann, then I update `src/_data/webinars.js` (trivial — it's just the ordered array). Decide FEHB/Medicare → November for Open Season. (2) Open PR + merge → Vercel deploy; then `curl -sI https://www.capitalwealth.com/webinars/` should 200 and the Federal Benefits nav should show "Federal Webinar Series." (3) **Per-event build, as each comes up:** create the Zoom webinar + SF campaign + `.ics`, re-point `/l/federal-benefits-webinar/` + `src/_data/webinar.js` + Vercel `ZOOM_WEBINAR_*` envs, and flip that episode's `status` to `"next"` (and the prior one to `"past"` with its YouTube id). (4) Optional: log the series + hub to Notion once the schedule is locked with Ann (held intentionally — no point publishing a schedule that's about to change in the Ann review).

### 2026-05-21 — Team + Ann strategy sync captured to Notion (webinar cadence → every 2 weeks)

- **What this was:** A working session with Mike, Josh, Bryce, Ann, Chad, and Kayden (recorded the afternoon of an in-person federal workshop). Bryce handed over the raw transcript and asked to capture it into Notion before sitting with Ann to review. **No code changed this session** — pure meeting-capture + planning.
- **Notion page created:** [🗓️ Team + Ann Sync — May 21, 2026 · Webinar Cadence, Future Pipeline & Sales Process](https://www.notion.so/3677def600a381b0b7f4ffa00ec4a1c8), under the [📁 Capital Wealth](https://www.notion.so/33c7def600a38183994aeb987e00f214) hub, and added to the hub's Quick Links. Structured into 8 decision areas + an 11-row action-items table with owners.
- **HEADLINE decision (Mike's call):** webinars move to **every 2 weeks, on Thursdays** (overrides the "every 3 weeks" Ann/Bryce floated). Rationale: build momentum, free to run, "ready, fire, aim" — would rather scale back than under-market. Next = **May 28 VERA/VSIP/RIF** (already built, registrations flowing), then TSP, then through Ann's 12–13 topic list. **Bryce to build a public webinar calendar page** listing all topics + dates.
- **Other decisions captured:** (1) Conversion — not volume — is the priority (May 14 funnel: 427 reg → ~99 attended → ~3% booked discovery; treat as baseline to trend). (2) New SF **single "Future / Nurturing" pipeline** for out-of-window prospects, close-date-driven, rolling ~120-day window — Kayden builds. (3) New **"Campaigns 3" Pipeline/ROI dashboard** + audit of existing (inaccurate) dashboards + remove blue-sheet tile F4 — Kayden. (4) Federal **sales process re-sequenced**: 0.5 meeting (qualify+book) → Comparison (Meeting 1) → Plan/Retirement Analyzer (Meeting 2); Chad is running the "general-public close" not the "federal close" — Mike+Josh to lock it with Ann while she's in town. (5) **Owned-email channel** — grab ~3M federal emails, batches of ~50 via SendGrid, 7am send, "we speak federal" angle — Bryce researches deliverability + builds plan. (6) Monthly in-person **Lunch & Learn** at libraries near agencies; blue-sheet wording → "Federal Benefit Gap" newsletter; tightened in-person CTA.
- **What to pick up next:** (1) Bryce reviews the Notion page with Ann + confirms her in-person dates and the topic sequence after May 28. (2) Bryce builds the public webinar calendar page on the site (every-2-weeks Thursday lineup). (3) Hand the SF pieces (Future pipeline, Campaigns 3 dashboard, dashboard accuracy audit) to Kayden. (4) Mike+Josh+Ann lock the federal sales process. (5) Bryce starts the owned-email deliverability research. Full owner list is in the action-items table on the Notion page.

### 2026-05-20 (PM) — May 28 webinar: Zoom created, Vercel envs set, $8K FB campaign built + verified end-to-end

- **Zoom webinar created via API:** `84887935919`, registration URL `https://capitalwealth.zoom.us/webinar/register/WN_S3G_gC0bQzqzLb7au9Wwcw`. Topic "Federal Benefits Webinar — VERA, VSIP & RIF — May 28", 5–6 PM MT (7 PM ET), 60 min, auto-approve, `registrants_confirmation_email: false` (matches May 14 — Zoom Events handles reminders), auto-record cloud, Q&A enabled.
- **Vercel production envs updated** (under `gull-stack` team — `brycedmorgan` DOES have access despite May 18 session log saying otherwise; just had to `vercel link --scope gull-stack --project capital-wealth`): `ZOOM_WEBINAR_ID = 84887935919`, `ZOOM_WEBINAR_URL = <reg URL>` (was missing entirely — first time it's been set). Redeployed via `vercel redeploy https://capital-wealth-641te5ni0-gull-stack.vercel.app --scope gull-stack` (24s). Verified new URL renders on `/l/federal-benefits-webinar/ty/`.
- **End-to-end test passed:** Bryce submitted `brycedmorgan+may28test@gmail.com` via the live form. SF Lead created with `LeadSource=Webinar`, `Campaign__c=701VS00000fQz8OYAS`, `Are_You_Federal__c=Yes`, Description appended with `Zoom Registrant ID: 2RcYHyNsSO2riNkejlRvcA` + Zoom Join URL containing the new webinar ID `84887935919`. Internal notification email landed in Bryce's inbox. Test lead deleted from SF. **Zoom registrant cancel via API failed (`webinar:update:registrant_status` scope missing on S2S app)** — Bryce to manually delete via Zoom UI or leave (will only result in one reminder email to `brycedmorgan+may28test@gmail.com`).
- **FB campaign `120242455510520665`** ("CW — May 28 Webinar — VERA/VSIP/RIF — Leads") built in PAUSED state, no SAC, $8K total across 8 days flat ($1K/day): AS1 `120242455510650665` $600/day on **NEW** LAL `120242455388620665` ("CW LAL 1% US - Paid Social Converters" — built from SF query of 276 HasResponded=TRUE members across 4 fed campaigns), AS2 `120242455513550665` $400/day on existing LAL `120241513320320665` ("Lookalike (1%) - Fed Employees - Source List") as control. Geo: US ex HI (`3854`)/CO (`3848`)/NM (`3874`). Excludes CRM List + both source seeds. 6 ads = 3 angles (VERA/VSIP/RIF Urgency, Offer Packet Isn't the Paycheck, Thirty Years On The Line) × 2 ad sets, each with unique brand-truth-compliant body copy following the "Federal employees within 10 years of retirement: [problem] → Ann's credibility → event details → topic list → 'Educational, no high-pressure pitch' → Reserve your seat ↓" winning template (decoded from May 14's $18.84 CPL Ann-authority winner). Reference image hashes for all 3 angles × 3 sizes (1:1, 4:5 primary, 9:16) saved in [Federal Webinar Ad Playbook](https://www.notion.so/3667def600a3813cb422f25f8969acfd).
- **May 14 retro learnings baked into the playbook:** Real CPL was **$21**, NOT the $10.80 the prior session log claimed (verified via `$8,524 spend ÷ 406 pixel leads = $20.99`, and `$5,153 SF ActualCost ÷ 245 webinar registrants = $21.03`). Ann-face won (70% of spend went to Ann-authority creative at $18.84 CPL). Specific dollar figures got 5.48% CTR but 50% worse CPL than the Ann-authority. Listicle format underperformed (2.10% CTR). May 14 ad copy had ~10 SEC Marketing Rule violations ("six figures over 20 years", "unlock secrets", "no products sold") — full forbidden-phrase list documented in the playbook.
- **PR #44 hotfix wave applied to all `/l/` federal landing pages today (May 20):** Original PR #44 missed 18 "No products sold" instances on the federal-webinar / 3-workshops / federal-workshops-hub / federal-review / federal-checklist / 10-things pages + the data file `src/_data/landing_pages/10-things.json`. Hotfix commit `58a0cc11` replaced all with "Educational, no high-pressure pitch" / "Educational, no sales pitch" framing. The orphaned `_site/services/portfolio-management-converted/` (no `src/` source, leftover from Bot Melvin commit) was patched directly.
- **Notion updates:** Created [📘 Federal Webinar Ad Playbook](https://www.notion.so/3667def600a3813cb422f25f8969acfd) under the Capital Wealth project hub — canonical reference for next campaign. Updated 4 existing pages (project hub callout, May 28 campaign hub, Landing Page Copy, Ad Copy, Campaign README) to scrub `fee-only` / `no products sold` / `unlock secrets` directives. Added image-size matrix (1:1 / 4:5 / 9:16 specs), 3 filled-in image prompts, and the emotional-triad creative observation (Urgency / Reality Check / Editorial).
- **What to pick up next:** (1) Merge [PR #44](https://github.com/Gull-Stack/capital-wealth/pull/44) (this commit's parent branch contains it + all today's hotfixes + the new FB build script). (2) After Vercel deploy: visit `/l/federal-benefits-webinar/` and confirm "Educational, no pitch" replaced "No products sold" on the live page. (3) Flip campaign + 2 ad sets + 6 ads to ACTIVE — easiest via Ads Manager UI, or I can write a one-line API script. (4) Monitor first-48-hour CPL via `node scripts/fb-diagnose.js`; if AS1 (new LAL) outperforms AS2 (control) by >20% in CPL, shift budget toward AS1. (5) **Rotate `ZOOM_CLIENT_SECRET`** — exposed in plaintext chat this session (per the long-running carry-forward item). (6) Manually cancel test Zoom registrant `brycedmorgan+may28test@gmail.com` via Zoom UI or skip (low impact). (7) Audit Salesforce email templates for "fee-only" / "no products sold" language (Josh — carried forward). (8) Add the May 28 SF campaign `701VS00000fQz8OYAS` to the Marketing dashboard webinar bucket if per-event reporting is wanted.

### 2026-05-20 — Federal Retirement Training slide deck shipped at /workshop/

- **What shipped:** ~54-slide federal retirement training presentation at `capitalwealth.com/workshop`, designed via Claude Design and dropped in as a self-contained bundle. Ann Werts uses this for in-person workshops — opens the URL on her laptop, clicks the new **Fullscreen** button (or presses **F**), and advances with her clicker (← / → / Space / PgUp / PgDn — all already supported by the deck's `<deck-stage>` web component).
- **Files added under [src/workshop/](../src/workshop/):** `index.html` (the v3 deck, 100KB, 53 slides at 1920×1080), `slides.css`, `deck-stage.js`, and `assets/` (20 images — 8 actively referenced, 12 portable extras). All copied verbatim from the Claude Design handoff bundle. The deck has its own `<head>` (Cormorant Garamond + Inter via Google Fonts, federal red/blue palette per the design tool's three-chat iteration with Bryce).
- **The only edits to the design output:** (a) added `<meta name="robots" content="noindex,nofollow" />` + a viewport meta to the deck's `<head>`. (b) Patched `deck-stage.js` (the design bundle's custom web component) to add a **Fullscreen toggle button** in its bottom overlay + **F key** binding, with a `_toggleFullscreen()` method that calls `document.documentElement.requestFullscreen()`. All other clicker keys (←/→/Space/PgUp/PgDn/Home/End/R/0–9) and the existing print-to-PDF mode + mobile tap zones + auto-scaling letterbox were already built into the component — no other changes needed.
- **Eleventy wiring:** Two lines added to [.eleventy.js](../.eleventy.js): `addPassthroughCopy("src/workshop")` + `ignores.add("src/workshop/**")`. Combo passes the directory through byte-for-byte and prevents Eleventy 2.x from template-processing the deck's `.html` (default engine for `.html` is Liquid). Verified the deck has zero `{{` or `{%` tokens, so a slip wouldn't break anything — but ignoring is clean.
- **Verification (local build, before push):** `npx @11ty/eleventy` writes `_site/workshop/{index.html,slides.css,deck-stage.js,assets/*}`. Slide count = 53 (`grep -c 'data-label='`). All 8 referenced assets present (zero missing in `comm -23`). Fullscreen patches in place (button HTML, click handler, F-key branch, method). Noindex meta + viewport in HTML.
- **What to pick up next:** (1) Commit + open PR + merge. (2) After Vercel deploy: `curl -sI https://www.capitalwealth.com/workshop/` should 200; visit in a desktop browser and verify the cover slide renders ("Federal retirement, *decoded.*" headline + Ann Werts portrait area), then arrow through to make sure the federal-flag stripe / navy header / page counters all look right. (3) Have Ann do a dry run with her clicker before the next workshop — confirm the Fullscreen button is discoverable and F-key works on her hardware. (4) If she wants speaker notes / two-monitor presenter view later, that's a follow-up — the v3 deck has no notes content written, and the underlying `<deck-stage>` already has speaker-notes infrastructure (`<script type="application/json" id="speaker-notes">`) we could wire up to a second window.

### 2026-05-19 (Late) — Compliance: corrected "fee-only" → "fee-based fiduciary specializing in federal benefits" site-wide

- **What shipped ([PR #44](https://github.com/Gull-Stack/capital-wealth/pull/44)):** Site previously claimed CW was a **"fee-only fiduciary"** in ~30 places across 15 source files — homepage `<title>` + meta description, site-wide footer (every page), `/start/` "No Sales Pitch" card (the trigger for this session — user surfaced via screenshot of an iMessage from a teammate), `/services/federal-benefits/` FAQ schema (which Google parses into rich results), `/faq/` visible accordion + schema, `/about/`, `/brand-facts/`, `/reviews/`, `/financial-advisor-utah/`, `/financial-advisor-idaho/`, `/services/portfolio-management/`, `/services/annuities/`, Lehi guide, `/l/retire-idaho/`, plus two blog posts (`planning-large-iras`, `roth-conversions`). User confirmed verbally CW is **fee-based** — earns advisory fees AND can earn insurance commissions via affiliate **CWA Insurance Services, LLC** (already in the every-page compliance footer disclosure). So the rest of the site was misaligned with what the footer already conceded — a real misrepresentation risk under NAPFA/CFP/SEC term-of-art rules.
- **Standard replacement** applied: `fee-only fiduciary` → `fee-based fiduciary specializing in federal benefits`; **also removed** all related false claims: "no commissions, no products to push", "we don't sell insurance products", "no proprietary products", "no incentive other than…", "fee-only compensation", "no product sales", "we don't earn commissions from product sales". Where those false-claim sentences were structural (e.g. JSON-LD FAQ answers, "Are you a fiduciary?" cards), rewrote to honest "fee-based advisory; any insurance commission through CWA Insurance Services, LLC is separately disclosed and optional" framing. **Kept** all "fiduciary" claims (still true). **Kept** the educational guide pages that *define* fee-only vs fee-based generically — definitions are accurate, they just shouldn't imply CW is fee-only.
- **Blog gotcha worth knowing:** [src/blog/roth-conversions-oct-11-2025.md](../src/blog/roth-conversions-oct-11-2025.md) has unusual non-ISO ASCII encoding that hides matches from plain `grep`. Verification fee-only sweep initially returned clean on `src/`, but `_site/blog/roth-conversions-oct-11-2025/index.html` still had `Comprehensive fee-only planning available`. Found the source by switching to `grep -a` (force binary-text mode). Same encoding may affect future global text sweeps over this file — use `grep -a` to be safe.
- **Verification:** `grep -rni "fee-only\|fee only" src/` returns only the three educational guide pages (intentionally preserved). Same grep on `_site/` matches the same three only. Rebuild via `npm run build` clean.
- **Out of scope (flagged in PR #44):** (a) FB ad creative text in Meta Ads Manager — live campaigns `120241962930110665` Federal Workshops + `120241615030540665` Federal Webinar need a separate audit; recommend pausing them until that's done. (b) Salesforce email templates sent via Apex batch jobs — Josh should hunt with `sf data query` on EmailTemplate. (c) "CW Marketing Hub" Lightning App Page — verify Looker Studio embed copy / SF dashboard rich-text components don't repeat the claim. (d) GMB / Google Business Profile / Apple Business / Bing Places — manual review needed. (e) Existing podcasts / YouTube / KSL/KNRS radio ads — historical, can't be edited.
- **What to pick up next:** (1) Review + merge [PR #44](https://github.com/Gull-Stack/capital-wealth/pull/44). (2) After Vercel deploy: hit homepage / footer / `/start/` "No Sales Pitch" card / `/services/federal-benefits/` and `/faq/` JSON-LD via DevTools — confirm zero `fee-only` hits. (3) Run Google [Rich Results Test](https://search.google.com/test/rich-results) on `/services/federal-benefits/` and `/faq/` — confirm FAQ schema parses cleanly with the new "fee-based" language. (4) **Pause live FB ad campaigns** (or schedule pause) until ad copy is audited in Ads Manager. (5) Tell Josh to audit Salesforce EmailTemplate records for "fee-only" language. (6) Consider submitting URL inspection re-index in GSC for the homepage to refresh Google's cached snippet faster than the next natural crawl.
- **Carried forward from prior session (still open):** (a) Create the May 28 Zoom webinar + update Vercel envs `ZOOM_WEBINAR_ID` / `ZOOM_WEBINAR_URL`; (b) Submit a test registration after deploy; (c) Promote the May 28 VERA/VSIP/RIF webinar (FB ad + email blast); (d) Add the new SF campaign to the Marketing dashboard webinar bucket; (e) The library DNS-filter issue from earlier today — consider categorization-dispute submissions to major filter vendors if it recurs.

- **What shipped:** ~54-slide federal retirement training presentation at `capitalwealth.com/workshop`, designed via Claude Design and dropped in as a self-contained bundle. Ann Werts uses this for in-person workshops — opens the URL on her laptop, clicks the new **Fullscreen** button (or presses **F**), and advances with her clicker (← / → / Space / PgUp / PgDn — all already supported by the deck's `<deck-stage>` web component).
- **Files added under [src/workshop/](../src/workshop/):** `index.html` (the v3 deck, 100KB, 53 slides at 1920×1080), `slides.css`, `deck-stage.js`, and `assets/` (20 images — 8 actively referenced, 12 portable extras). All copied verbatim from the Claude Design handoff bundle. The deck has its own `<head>` (Cormorant Garamond + Inter via Google Fonts, federal red/blue palette per the design tool's three-chat iteration with Bryce).
- **The only edits to the design output:** (a) added `<meta name="robots" content="noindex,nofollow" />` + a viewport meta to the deck's `<head>`. (b) Patched `deck-stage.js` (the design bundle's custom web component) to add a **Fullscreen toggle button** in its bottom overlay + **F key** binding, with a `_toggleFullscreen()` method that calls `document.documentElement.requestFullscreen()`. All other clicker keys (←/→/Space/PgUp/PgDn/Home/End/R/0–9) and the existing print-to-PDF mode + mobile tap zones + auto-scaling letterbox were already built into the component — no other changes needed.
- **Eleventy wiring:** Two lines added to [.eleventy.js](../.eleventy.js): `addPassthroughCopy("src/workshop")` + `ignores.add("src/workshop/**")`. Combo passes the directory through byte-for-byte and prevents Eleventy 2.x from template-processing the deck's `.html` (default engine for `.html` is Liquid). Verified the deck has zero `{{` or `{%` tokens, so a slip wouldn't break anything — but ignoring is clean.
- **Verification (local build, before push):** `npx @11ty/eleventy` writes `_site/workshop/{index.html,slides.css,deck-stage.js,assets/*}`. Slide count = 53 (`grep -c 'data-label='`). All 8 referenced assets present (zero missing in `comm -23`). Fullscreen patches in place (button HTML, click handler, F-key branch, method). Noindex meta + viewport in HTML.
- **What to pick up next:** (1) Commit + open PR + merge. (2) After Vercel deploy: `curl -sI https://www.capitalwealth.com/workshop/` should 200; visit in a desktop browser and verify the cover slide renders ("Federal retirement, *decoded.*" headline + Ann Werts portrait area), then arrow through to make sure the federal-flag stripe / navy header / page counters all look right. (3) Have Ann do a dry run with her clicker before the next workshop — confirm the Fullscreen button is discoverable and F-key works on her hardware. (4) If she wants speaker notes / two-monitor presenter view later, that's a follow-up — the v3 deck has no notes content written, and the underlying `<deck-stage>` already has speaker-notes infrastructure (`<script type="application/json" id="speaker-notes">`) we could wire up to a second window.

### 2026-05-19 (PM) — FERS webinar replay page shipped at /blog/fers-pension-explained/

- **What shipped:** New page [src/blog/fers-pension-explained/index.njk](../src/blog/fers-pension-explained/) (URL `/blog/fers-pension-explained/`) — the May 14 webinar repurposed into a long-form, SEO-targeted blog/replay page. Embeds the YouTube video ([Bd-eW4XrztA](https://www.youtube.com/watch?v=Bd-eW4XrztA)) at the top, then "What you'll learn," 5 takeaways, chapter markers, **15 expandable FAQ entries**, and the gold CTA to `/start`. Three JSON-LD blocks: `VideoObject`, `Article`, `FAQPage` — the FAQPage covers all 15 Q&A for Google rich results + AI answer engines.
- **SEO target:** Title "FERS Pension Explained: How to Calculate Your Federal Retirement (Webinar)" — maps to real keyword volume (FERS pension explained / how to calculate FERS pension, ~10K+ monthly). Description weights for the long-tail terms. Page body is ~6K words rendered (incl. layout chrome) with substantial new copy (TOC + expanded takeaways + 15 FAQ pairs).
- **Internal link added** from the existing FERS pension blog post ([src/blog/fers-pension-calculation-guide-apr-24-2026.md](../src/blog/fers-pension-calculation-guide-apr-24-2026.md)) to the new replay — passes authority from the existing calc post to the new page.
- **YouTube embed:** uses `youtube-nocookie.com` for privacy; lazy-loaded; primary CTA is "Book a 30-min Call" with a ghost "Watch on YouTube" as secondary.
- **Why /blog/ vs /federal-university/:** Bryce asked the right question — the `/federal-university/` hub doesn't exist yet (no parent index, no nav), so the page was orphaned. Moved to `/blog/fers-pension-explained/` — clean keyword slug, lives on the existing blog rails. When the hub launches, the webinars library can link to this URL directly.
- **What to pick up next:** (1) Add a YouTube-branded thumbnail (prompt provided this session). (2) Add internal links to the replay from `/services/federal-benefits/`, the FERS calculator, `/federal-resources` (a separate pass — they pass authority to the new page). (3) Set up a softer email-gated capture mid-page (the planned Federal Retirement Documents Checklist would fit) to lower the conversion friction below the phone-call CTA. (4) When the hub launches, decide whether to keep the canonical URL here or 301 to `/federal-university/webinars/...`.

### 2026-05-19 — Webinar PR opened + content initiative scoped into Asana

- **What shipped:** [PR #40](https://github.com/Gull-Stack/capital-wealth/pull/40) opened for the FERS recap email + static resource (branch `claude/elated-johnson-017254`, commit `5f3e2cf7`).
- **Asana — repurposing the webinar into content:** Created a parent task **"[Webinar] Repurpose the May 14 FERS webinar into hub content"** ([task 1214932127206247](https://app.asana.com/1/1207994197998140/project/1213980435957819/task/1214932127206247)) in the **Federal Education Hub** project (`1213980435957819`), with 8 subtasks, each containing **drafted content** ready for Mike's review: (1) audit current federal content, (2) publish the recording to the site, (3) FERS FAQ page from the webinar Q&A — 15 Q&A pairs drafted, (4) refresh the FERS pension blog post, (5) 8 social posts drafted, (6) 6 video-clip specs with timestamps, (7) Federal Retirement Documents Checklist content, (8) 3-email registrant nurture sequence.
- **Key context:** The Federal Education Hub project already had 27 well-planned tasks (calculators, pillar articles, gated guides, checklists, webinar pages, nurture A/B). The webinar subtasks are deliberately scoped to *feed* that hub, not duplicate it. Notably a FERS pension calculation blog post already exists ([src/blog/fers-pension-calculation-guide-apr-24-2026.md](../src/blog/fers-pension-calculation-guide-apr-24-2026.md)) — hence "refresh" not "new article". `/services/federal-benefits/` is an existing 11-page section using `faqSchema` frontmatter.
- **Asana gotcha:** the Asana MCP `html_notes` field rejects all input with "XML is invalid" (even `<body><p>test</p></body>`). Use plain-text `notes` instead.
- **What to pick up next:** (1) Merge [PR #40](https://github.com/Gull-Stack/capital-wealth/pull/40). (2) Mike reviews the 8 subtask drafts for accuracy. (3) Execute the subtasks (FAQ page + blog refresh are the highest SEO value). (4) This session-log entry is an uncommitted change to `.claude/CLAUDE.md`.

### 2026-05-18 — FERS webinar recap email built + sent to 428 registrants

- **What shipped:** New email [emails/fers-webinar-recap-2026-05.html](../emails/fers-webinar-recap-2026-05.html) — a recap of the May 14 "The Federal Benefits Gap" FERS pension webinar. First email asset in the repo (no `emails/` dir existed before). Tight digest: 5 takeaways (keep your records / when you can retire / the pension formula / sick leave counts / protect your spouse) consolidated from the webinar transcript, a **webinar replay** block (Zoom recording link), and a CTA to `capitalwealth.com/start` to book a 30-minute call. Built to the CLAUDE.md Email Template Standards: 680px tables, light-mode lock, HTML entities, navy `#0f2742` header + gold `#fdd25e` CTA, both compliance disclaimers, address, unsubscribe.
- **SENT:** Campaign emailed to **all 428 members** of SF Campaign `701VS00000eWrYVYA0` (the May 14 webinar) from vip@capitalwealth.com OWA (`0D2VS000000L1p70AC`). All 428 `SendEmailResult.isSuccess()=true`, **0 failures**; 428 activities logged on the Lead/Contact records (`setSaveAsActivity`). Opens tracked natively by Salesforce. 1 prior test also went to bmorgan@capitalwealth.com.
- **Recording:** Zoom cloud recording of meeting `82502463485` ("Federal Benefits Webinar: Live Online Masterclass", 92 min) — share URL with `?pwd=` play-passcode embedded for one-click access. Pulled via Zoom MCP `recordings_list`.
- **How it was sent (gotchas for next time):** Synchronous anonymous Apex `Messaging.sendEmail` with an 18KB HTML body costs ~488ms CPU per email — 428-at-once and even 40-at-once blew the 10s Apex CPU limit. Solution: HTML staged as a **static resource** [salesforce/.../staticresources/CW_FERS_Recap_Email](../salesforce/force-app/main/default/staticresources/) (the email HTML itself exceeded the anonymous-Apex script size limit when base64-inlined), then sent in **35 chunks of 12** via separate `sf apex run` transactions (each ~6s CPU). For future bulk sends, prefer List Email (UI) or a Batch Apex class over synchronous anonymous Apex.
- **Notes:** Presenter is "Ann Werts" per the team list (transcript auto-captioned "Ann Wurtz"). Greeting is generic "Hi there,". Per-link click tracking was scoped OUT (Bryce chose opens-only) — List Email/Apex track opens but not clicks natively.
- **What to pick up next:** (1) New uncommitted files — `emails/fers-webinar-recap-2026-05.html` and the `CW_FERS_Recap_Email` static resource (static resource is already deployed to the `cw` org). Commit if desired. (2) In a day or two, check open rate and `/start` bookings attributable to this send. (3) Spot-check the recording link from a recipient's inbox to confirm Zoom share settings allow external viewing.

### 2026-05-19 — Reverted `/start/` to Federal Benefit Comparison framing + diagnosed library DNS block

- **What shipped ([PR #41](https://github.com/Gull-Stack/capital-wealth/pull/41)):** Reverted [PR #37](https://github.com/Gull-Stack/capital-wealth/pull/37) on `/start/` — restored federal-employee retirement focus (FERS/TSP/FEHB/FEGLI in copy, "Federal Benefit Comparison" as the named offer, American-flag ogImage, federal title + description). Also updated hero H1 from "Let's start your Federal Benefit Comparison" → **"Request your Federal Benefit Comparison"** for stronger CTA framing. Clean `git revert 6102ed25 --no-commit` + single H1 Edit on both [src/start/index.njk:388](../src/start/index.njk:388) and [_site/start/index.html:973](../_site/start/index.html:973).
- **Why the revert:** `/start/` is the landing target for federal-focused FB ads + email outreach. PR #37 (May 18) generalized it into a "retirement review" page which dilutes the offer for the audience that actually lands there. The Zoom scheduler URL (`scheduler.zoom.us/.../federal-benefits-30-minute-phone-call`) was always federally-named — and stays that way, no Zoom-side change needed.
- **Verification passed:** 14 federal references in src/start, 1 "Request your" hit, 0 "retirement review" hits.
- **Separate issue diagnosed (not a site bug):** User reported the site appeared down. It wasn't — Vercel was serving HTTP/2 200 from correct IPs. Public DNS (Google `8.8.8.8`, Cloudflare `1.1.1.1`) returned correct Vercel CNAME chain. The user was on a public library's filtered Wi-Fi where the resolver at `172.16.1.191` returned `0.0.0.0` for `www.capitalwealth.com` — the canonical signature of a DNS-level content filter blocking the domain (likely "Financial Services" category match or Vercel CNAME-chain reputation hit). Apex `capitalwealth.com` was unaffected; only `www` was blocked. User went to ask library IT to allowlist `www.capitalwealth.com` and `capitalwealth.com`.
- **Worth-knowing pattern:** Library/school/hospital DNS filters returning `0.0.0.0` for `www.capitalwealth.com` will silently lose potential federal clients trying to reach us from those networks (CIPA-compliant filters are ubiquitous in those environments). Worth submitting a categorization-dispute form to whichever filter vendor the library uses — that propagates the unblock to every site running the same vendor. Not done this session; flagging for future consideration.
- **What to pick up next:** (1) Merge [PR #41](https://github.com/Gull-Stack/capital-wealth/pull/41). (2) After Vercel deploy: visit `https://www.capitalwealth.com/start/`, verify hero reads "Request your Federal Benefit Comparison", team strip + value card #3 + "What we'll cover" all show federal copy; click a Zoom slot end-to-end to confirm the embed still books. (3) If the library DNS block is a recurring pattern across multiple library networks, submit reclassification disputes to major filter vendors (Cisco Umbrella, Fortinet, Securly, etc.).
- **Carried forward from prior session (still open):** (a) Create the May 28 Zoom webinar + update Vercel envs `ZOOM_WEBINAR_ID` / `ZOOM_WEBINAR_URL`; (b) Submit a test registration after deploy; (c) Promote the May 28 VERA/VSIP/RIF webinar (FB ad + email blast); (d) Add the new SF campaign to the Marketing dashboard webinar bucket.

### 2026-05-18 — Scheduled next federal webinar: VERA/VSIP/RIF, Thu May 28

- **What shipped:** The evergreen webinar landing page `/l/federal-benefits-webinar/` (and its `/ty/` thank-you page) was re-pointed from the past May 14 broad-overview webinar to the **next** one — **Thursday, May 28, 2026, 6:00–8:00 PM MT**, topic **"VERA, VSIP & RIF — Evaluating an Early-Out Offer"** (chosen from Ann Werts' 2026 topic menu; user picked this for timeliness given the federal workforce climate). Updated **in place** — generic slug stays evergreen, May 14 event is past, page is indexable. No redirect, no new URL.
- **Files changed (8, all webinar-scoped):** [src/l/federal-benefits-webinar/index.njk](../src/l/federal-benefits-webinar/index.njk) (frontmatter title/desc/Event schema/faqSchema, hidden form fields, hero/outcomes/testimony/instructor/FAQ/CTA copy rewritten for the VERA/VSIP/RIF angle, analytics labels); [src/l/federal-benefits-webinar/ty/index.njk](../src/l/federal-benefits-webinar/ty/index.njk) (dates, data-key, pixel labels, added a Topic row); [src/_data/webinar.js](../src/_data/webinar.js) (`may14FederalBenefits` key → `may28FederalBenefits`); [api/submit-lead.js](../api/submit-lead.js) (`isFederalWebinar` email subject + body + internal-notification dates → May 28); new [src/assets/webinars/federal-benefits-webinar-2026-05-28.ics](../src/assets/webinars/federal-benefits-webinar-2026-05-28.ics). Plus regenerated `_site/` build output for the two pages + the new `.ics` (unrelated `_site/sitemap.xml` reorder reverted, per the prior-session convention).
- **Fixed a pre-existing inconsistency:** the May 14 landing page hero said "One hour / 60-minute / 6:00–7:00 PM" while its own `.ics`, confirmation email, and TY page all said 6:00–8:00 PM (2 hrs). The new page standardizes on **2 hours** everywhere.
- **Salesforce campaign created:** `701VS00000fQz8OYAS` — "Federal Benefits Webinar 5.28.26 — VERA, VSIP & RIF" (Type `Federal Benefits`, Status `In Progress`, modeled on the May 14 campaign `701VS00000eWrYVYA0`). Wired into the page's `campaign_id` hidden field.
- **Zoom webinar NOT created — blocked:** the Zoom S2S OAuth creds live only in the Capital Wealth Vercel project's env, and this machine's Vercel login (`brycedmorgan`) has no access to that project (only teams `gull-stack` / `prestondixon23s-projects`, neither holds it). Code references `process.env.ZOOM_WEBINAR_ID` / `ZOOM_WEBINAR_URL` only — nothing hardcoded — so the code is complete regardless.
- **What to pick up next:** (1) Open PR for this branch + merge to `main`. (2) **Create the new Zoom webinar** for May 28 (registration required, `approval_type 0`, `registrants_confirmation_email: false` — same as May 14). (3) **Update Vercel production env:** `ZOOM_WEBINAR_ID` → new webinar id, `ZOOM_WEBINAR_URL` → new join/registration URL — then redeploy so `webinar.js` picks them up. Without this, registrations create SF Leads but no Zoom registrant (same silent-failure bug as the May 5–11 window). (4) After deploy: submit a test reg, confirm TY page + `.ics` + confirmation email say May 28, and `sf data query` shows the lead on campaign `701VS00000fQz8OYAS`; delete the test lead + Zoom registrant. (5) Promote it — FB ad campaign + email blast; the VERA/VSIP/RIF angle is the timely hook. (6) Add the new SF campaign to the Marketing dashboard webinar bucket if per-event reporting is wanted.

### 2026-05-18 — Aliased `/schedule-a-meeting/` → `/contact/` (301)

- **What shipped:** Two paired 301 redirect entries added to [vercel.json](../vercel.json) so `capitalwealth.com/schedule-a-meeting/` (and the no-slash form) resolve to the existing `/contact/` page. Placed right after the `/l/federal-phase-one` pair. `permanent: true` → Vercel emits a 308.
- **Why an alias, not a move:** `/contact/` is already the site's "Schedule Your Visit" page (title is literally "Contact Capital Wealth | Schedule Your Visit") and is linked from ~70 files site-wide (nav, footers, every service page). User confirmed: keep `/contact/` where it is, just alias the new marketing URL. No file moves, no internal-link changes, no `_site/` rebuild (`vercel.json` is config, not build output).
- **What to pick up next:** (1) Open PR for this change. (2) After Vercel deploy: `curl -sI https://www.capitalwealth.com/schedule-a-meeting/` should return 308 with `location: /contact/`; same for the no-slash form; `/contact/` itself should still 200 directly.

### 2026-05-17 — Marketing dashboard (Dashboard 3 of the SF Dashboard Overhaul): "MARKETING: Spend & ROI" + automated FB ad-spend sync

- **Context:** Mike circulated `SF Dashboard Checklist.pdf` (4-dashboard overhaul — Campaigns, Opportunities, Marketing, Executive). Bryce owns **Dashboard 3: Marketing**. PDF asked to first confirm what cost data is in SF. Built the full thing per Bryce's call: new separate dashboard + automated FB spend sync. Plan file: `~/.claude/plans/users-bryce-downloads-sf-dashboard-chec-resilient-hanrahan.md`.
- **Cost-data finding:** `Campaign` already has rich cost infrastructure — `ActualCost`, itemized `Mailer/Venue/Food/Advertisement/Social/Speaker_Cost__c` → `Total_Campaign_Cost__c`, and formula fields `Cost_Per_Lead__c` / `Cost_Per_Won__c`. Only ~147/430 campaigns have cost populated; FB/Google ad campaigns showed **$0**. Crucially, per-campaign **funnel-count fields already exist** (`X1st_Sets__c`, `X1st_Kepts__c`, `X3rd_Kepts__c`, `Leads_Attended_Event__c`, `Onboarded_Accounts__c`, `Is_Bottom_Campaign__c`) — so the cost-per-stage tiles the plan thought were blocked were all buildable. **All 9 PDF Marketing tiles shipped.**
- **What shipped (all deployed live to SF org `cw`):** 9 reports `M1`–`M9` in folder `4 - Marketing Reports` + dashboard **MARKETING: Spend & ROI** (id `01ZVS000002uXhh2AE`, folder `4 - Marketing`). Reports use `reportType=CampaignList`; cost-per-stage tiles use report custom summary formulas (`ACTUAL_COST:SUM / Campaign.X1st_Kepts__c:SUM` etc.) so no new schema/FLS work was needed. Metadata version-controlled under [salesforce/force-app/main/default/](../salesforce/) (reports + `dashboards/X5MarketingMarketing/CW_Marketing_Spend_and_ROI.dashboard-meta.xml`).
- **FB ad-spend sync built:** [scripts/sf-sync-fb-spend.js](../scripts/sf-sync-fb-spend.js) (manual, `--dry-run`) + [api/sync-fb-spend.js](../api/sync-fb-spend.js) (Vercel cron, daily 07:00 UTC — added `crons` to [vercel.json](../vercel.json)). Mapping: SF `Campaign.Platform_Spend_Source__c` holds comma-separated `facebook:campaign:<id>` / `facebook:adset:<id>` tokens; sync pulls lifetime FB spend (campaign + ad-set level, `date_preset=maximum`) and writes the sum to `Campaign.ActualCost`. Workshop campaigns map to FB **ad sets** (per-city) since the FB workshop campaigns cover all 3 events. Dry-run confirmed working (pulls 11 FB campaigns / 22 ad sets).
- **BLOCKED — needs Bryce to run:** the one-time populate of `Platform_Spend_Source__c` on 6 campaigns was denied by the Claude Code write classifier. The exact 6 `sf data update record` commands + the FB-id mapping table are in [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md#facebook-ad-spend-sync). Until that runs, the sync has nothing to sync and the FB campaigns stay at $0 cost.
- **Flags raised in docs (need team sign-off):** (1) dashboard title `MARKETING: Spend & ROI` is proposed — PDF gave no title for Dashboard 3. (2) `ActualCost` vs itemized `Total_Campaign_Cost__c` are two parallel cost totals — dashboard standardizes on `ActualCost`; team should pick one canonical. (3) cost-per-stage funnel mapping ("Appointment Requested" = 1st Set, "Discovery Held" = 1st Kept, "Investment Held" = 3rd Kept) needs Josh's confirmation. (4) "vs. prior period" arrows not natively supported on all SF component types.
- **What to pick up next:** (1) ✅ DONE — ran the 6 `Platform_Spend_Source__c` updates + sync; FB campaigns now show real spend ($24,781 swept into SF, Webinar CPL $19.92, SLC workshop CPL $227 ⚠️). (2) Open the dashboard and confirm the cost-per Bar tiles render — the `FORMULA1` custom-summary-formula chartSummary reference and FLS on `X1st_*__c` fields for jcohen are the two unverified risks. (3) ✅ DONE — `CRON_SECRET` set in Vercel prod env. (4) ✅ DONE — PR #46 opened + merged. (5) Confirm the 4 flags with Josh/Mike. (6) Manual cost entry for Google Ads + radio campaigns.

### 2026-05-14 — Moved `/l/federal-phase-one/` → `/start/`

- **What shipped:** Page that lived at `/l/federal-phase-one/` (30-min federal benefits scheduler — Zoom embed above the fold, team strip, value props, "what we'll cover") now lives at the shorter, brandable `/start/`. Source file moved [src/l/federal-phase-one/index.njk](../src/l/federal-phase-one/index.njk) → [src/start/index.njk](../src/start/index.njk) with one frontmatter line changed: `permalink: "/start/"`. All page content (premium brand palette work from `d60ccdeb`, `4ff89526`, `f43a88ce`, `6eedbad2` earlier this week) carried over untouched.
- **301 redirect added** in [vercel.json](../vercel.json): `/l/federal-phase-one` and `/l/federal-phase-one/` → `/start/` with `permanent: true`. Paired-entry style matches the SDBA dead-URL cleanup from commit `aa9111ff`. Old URL still works for any FB ads, email links, or external references.
- **Preserved deliberately:** `noindex: true` + `excludeFromSitemap: true` stayed on. The page is a scheduler landing target (Zoom iframe + value props), not an SEO target — same posture as before. Confirmed `/start/` does NOT appear in `_site/sitemap.xml`. Title still says "Book a 30-Minute Federal Benefits Call | Capital Wealth" since the audience is still federal traffic from ads + email.
- **Repo cleanliness:** Recent commits in this repo include both `src/` and `_site/` outputs (e.g. `d60ccdeb` shipped `_site/l/federal-phase-one/index.html` alongside the source). Matched that pattern: built `_site/start/index.html` is staged, stale `_site/l/federal-phase-one/index.html` is removed. Reverted an unrelated `_site/sitemap.xml` reorder that the local rebuild surfaced (alphabetical reshuffle, no real content change).
- **No other repo refs to update:** grep across `src/`, `api/`, `scripts/`, `*.json` returned only the source file itself. Nothing in nav, layouts, sitemap, robots, FB ad scripts, or email templates points at `/l/federal-phase-one/`.
- **What to pick up next:** (1) Open PR for this change. (2) After Vercel deploy: `curl -sI https://www.capitalwealth.com/start/` should return 200; `curl -sI https://www.capitalwealth.com/l/federal-phase-one/` should return 308 with `Location: /start/`. (3) Sanity-click a Zoom slot end-to-end on the live `/start/` page to confirm the embed still books. (4) Consider whether any active FB ad creatives need their final URL repointed from `/l/federal-phase-one/` to `/start/` — redirect handles it, but a direct link is cleaner for ad-system attribution. (5) If `/start/` is going to start showing up in non-federal contexts, consider flipping `noindex: true` → `false` and tweaking the title from "Federal Benefits Call" to something more general.

### 2026-05-12 (Late) — Marketing Hub Lightning App Page: Looker Studio GA4 embedded inside Salesforce

- **One-pager Mike opens:** [https://capitalwealth.lightning.force.com/lightning/n/CW_Marketing_Hub](https://capitalwealth.lightning.force.com/lightning/n/CW_Marketing_Hub). GA4 brand-awareness embed on top (via Looker Studio iframe), gold "Open Dashboard" button to the existing CW — Marketing Live dashboard below.
- **Components shipped via SF CLI metadata API** (committed under [salesforce/force-app/main/default/](../salesforce/)): `CspTrustedSite/Looker_Studio` (whitelists `lookerstudio.google.com` for iframes), `ApexPage/CW_Brand_Awareness_Embed` (Visualforce shell iframing the published embed URL), `FlexiPage/CW_Marketing_Hub` (Lightning App Page, default-app-home template, single "main" region), `CustomTab/CW_Marketing_Hub` (icon `Custom20: Heart`, surfaces in App Launcher).
- **Looker Studio report:** [f08fd886-91f2-4c0f-a28b-179a3dba0dfb](https://lookerstudio.google.com/reporting/f08fd886-91f2-4c0f-a28b-179a3dba0dfb). GA4 property `375072851`. Bryce changed share setting + needs to finish: (1) File → Embed report → toggle ON (so the iframe renders data), (2) Add 3 charts — Scorecard for Sessions, Table for Page paths, Donut for Session source — per [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md#looker-studio-setup), (3) Share with `mstevens@capitalwealth.com` as Viewer.
- **Metadata gotchas this session (saving these for next time):** `CustomTab` icon element is `<motif>` not `<icon>` (deploy error "Invalid motif:null" was misleading); `<mobileReady>` not valid in CustomTab v66; `flexipage:dashboard` component fails deploy on AppPage type FlexiPages in this org with "couldn't retrieve design time component information" — so embedding a SF dashboard directly inside an App Page isn't possible here (worked around with a gold-button link to the dashboard URL).
- **No app navigation edit performed** — intentionally avoided modifying `CustomApplication/Capital_Wealth` since Mike has his own customized `Capital_Wealth_Mike` app with a tailored homepage and roundtripping the production app metadata is risky. Marketing Hub tab is accessible via App Launcher; each user can pin it to nav via the per-user pencil → "Edit Capital Wealth navigation items" (4 clicks), or admin can do it globally via Setup → App Manager → Capital Wealth → Edit → Navigation Items.
- **What to pick up next:** (1) Bryce finishes the 3 charts in Looker Studio + enables embed + shares with Mike. (2) Mike pins "Marketing Hub" to his nav bar via the pencil icon at the right edge of the nav. (3) Merge [PR #29](https://github.com/Gull-Stack/capital-wealth/pull/29) (contains all the SFDX metadata for the dashboard + reports + Marketing Hub). (4) (Carried forward) Watch the Federal Workshops Educational FB campaign CPL at the ~36 hour mark — if under ~$15, pause old SAC campaign `120241691882900665`. (5) (Carried forward) Send the 48 SendGrid backfill emails. (6) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (7) (Carried forward) Rotate Zoom + FB secrets sent in plaintext earlier.

### 2026-05-12 — Marketing dashboard fully deployed: 6 reports + dashboard live in SF via metadata API, backfill applied

- **Built and deployed entirely via Salesforce CLI metadata API** (not the Lightning UI): 6 reports (R1–R6) in `4 - Marketing Reports` folder + `CW — Marketing Live` dashboard (Id `01ZVS000002sdBJ2AY`) in `4 - Marketing` folder. Source XML committed at [salesforce/force-app/main/default/](../salesforce/). Dashboard runs as Josh (full-org visibility), folder is shared with All Internal Users by default so Mike already has view access — no extra grant needed. Dashboard URL: https://capitalwealth.lightning.force.com/lightning/r/Dashboard/01ZVS000002sdBJ2AY/view
- **Backfill applied successfully** via [scripts/sf-backfill-campaigns.js](../scripts/sf-backfill-campaigns.js): 258 Lead `Campaign__c` updates + 56 new CampaignMember rows (202 already existed from May 11 webinar backfill). 7 transient Hill AFB failures during the batch run, all retried successfully. Final lead counts in last 30 days: **Webinar 296, Ogden 11, Hill AFB 9, SLC 0, General 3.** SLC=0 is expected — no traffic to that page yet because the May 20 event date is the latest of the three workshops.
- **PR #28 merged + deployed** before the backfill ran. Confirmed live by curl'ing the Ogden landing page and verifying the `<input type="hidden" name="campaign_id" value="701VS00000eWgMsYAK">` is in the rendered HTML.
- **Dashboard component-type gotchas hit while deploying** (so the next session knows): SF metadata enum names for `DashboardComponentType` are NOT what they're called in the Lightning UI — `HorizontalBar` → must be `Bar`; `VerticalColumnStacked` → must be `ColumnStacked` (and it requires `autoselectColumnsFromReport=true` OR no `secondaryGroupingColumn`); `RichText` is NOT a valid metadata enum at all (must be added via Lightning UI); `Table` components require `indicatorLowColor`/`indicatorMiddleColor`/`indicatorHighColor` even though they're never displayed; `titleSize` only accepts {8, 9, 10, 12, 14, 18, 24, 36}; `sortColumn` is only valid in Tabular format reports (not Summary). Report metadata also can't reference grouping fields as columns AND group on them — choose one.
- **What I dropped from the original plan vs. what I shipped:** (a) skipped per-channel KPI metric cards — replaced with the simpler "Today by Channel" bar chart that conveys the same info. (b) Dropped inline `<chart>` blocks on R1-R6 reports because charts are rendered by the dashboard component, not the report. (c) Dropped the rich-text "Looker Studio link" component because it's not a valid metadata enum — to be added manually in the SF UI after the GA4 dashboard is built.
- **Looker Studio next step (Bryce, ~5 min):** click the Linking-API URL in [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md), approve GA4 access, add 3 charts (scorecard for Sessions, table for Page path, donut for Session source). GA4 property ID `375072851`. Share with `mstevens@capitalwealth.com`. Copy the URL back into the SF dashboard as a Rich Text component (instructions in the doc).
- **What to pick up next:** (1) Build the Looker Studio dashboard (5-min task). (2) Add the Rich Text component with the Looker URL to the top of the SF dashboard via Lightning UI. (3) Have Mike open the dashboard and confirm it answers what he wanted. (4) Watch the Federal Workshops educational FB campaign CPL at the ~36 hour mark — if under ~$15, pause the old SAC campaign `120241691882900665`. (5) (Carried forward) Send the 48 SendGrid backfill emails. (6) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (7) (Carried forward) Rotate Zoom + FB secrets sent in plaintext earlier.

### 2026-05-11 (Late PM) — Marketing dashboard foundation: wired federal-event pages to their SF Campaigns + built dashboard build guide

- **Why this came up:** Josh built a sales-funnel campaigns dashboard (Leads → Attended → Blue Sheet → Converted → Production). Mike asked for a separate **marketing** view: brand-awareness (GA4) + live lead counts split into 5 buckets — Webinar 5/14, Workshop Ogden 5/19, Workshop SLC 5/20, Workshop Hill AFB 5/21, and General Contact.
- **The blocker I found while planning:** today, all 4 federal-event landing pages AND the general contact form submit leads that land in the **same default Campaign** (`Website Form 2026` = `701VS00000dB91aYAC`) with `LeadSource='Website Form'` (or `'Webinar'` for webinar). The 5 buckets are **indistinguishable in structured SF data** — the only difference is `Description LIKE '%Event Date: 2026-05-19%'`, and **Description is a long-text area that cannot be filtered in SOQL** (`INVALID_FIELD: field 'Description' can not be filtered`). Without fixing this, no per-event report is possible.
- **What shipped in [PR #28](https://github.com/Gull-Stack/capital-wealth/pull/28):** (a) Added a hardcoded `<input type="hidden" name="campaign_id" value="…">` to each of the 4 event landing pages so [api/submit-lead.js:186](../api/submit-lead.js:186) (which already validates+uses `campaign_id`) routes new leads to the right SF Campaign on creation. Webinar → `701VS00000eWrYVYA0`, Ogden → `701VS00000eWgMsYAK`, SLC → `701VS00000eWe58YAC`, Hill AFB → `701VS00000eWqE8YAK`. (b) Patched [api/submit-lead.js](../api/submit-lead.js) to auto-set `Are_You_Federal__c='Yes'` on both insert AND existing-lead update when `isFederalLead` is true — previously this only got set via manual SF bulk updates, which left the Federal vs General segmentation broken for new submissions. (c) Added [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md) — click-by-click checklist for the 6 reports + 1 SF dashboard + 1 Looker Studio dashboard, including exact filter values, Campaign IDs, the navy/gold color hint, and Looker Studio widget specs.
- **One-time backfill staged but NOT run:** `tmp/backfill-campaigns.js` (gitignored). Dry-run found **268 leads** currently sitting in the default catch-all over the last 30 days: 245 Webinar (need Campaign__c flip — CampaignMember rows already exist from the May 11 backfill), 11 Ogden, 0 SLC, 9 Hill AFB, 3 unmatched (general contact, correctly stays in catch-all). Script also sets `Are_You_Federal__c='Yes'` on all 265 matched. Blocked on PR #28 merge + deploy — running before that would attach historical leads to campaigns whose form pages still send everything to the default, creating an inconsistent before/after state.
- **Why click-checklist instead of metadata XML for the SF dashboard:** SF Report metadata is verbose and brittle (each report = ~50 lines of nested XML for columns/groupings/filters/charts), this repo has no SFDX project set up, and Josh has already built one dashboard manually in ~hours so a checklist hand-off matches his workflow. Checklist puts Mike in front of working numbers in 60–90 min of Josh's time post-merge.
- **GA approach locked in:** Looker Studio dashboard (free, GA4-native) linked from a Rich Text component at the top of the SF dashboard. Not building a GA→SF custom object pipeline yet — half a day of cron+auth+schema work for a V1 that may or may not get used. If Mike loves the link-out, upgrade later.
- **What to pick up next:** (1) Review + merge [PR #28](https://github.com/Gull-Stack/capital-wealth/pull/28). (2) After Vercel deploys: `node tmp/backfill-campaigns.js --dry-run`, confirm counts, then `node tmp/backfill-campaigns.js` (no flag) to apply. (3) Josh follows [docs/MARKETING_DASHBOARD.md](../docs/MARKETING_DASHBOARD.md) — builds 6 reports in `4 - Marketing Reports` folder, assembles `CW — Marketing Live` dashboard with Running User=`jcohen@capitalwealth.com`. (4) Build the Looker Studio dashboard per the same doc, share with `mstevens@capitalwealth.com`, paste URL into the Rich Text component. (5) Submit 4 test leads (one from each event page) with email pattern `brycedmorgan+marketing-test-<bucket>@gmail.com` and verify with the SOQL query at the bottom of the doc. (6) Watch the existing FB Workshops educational campaign CPL for the **~36 hour** mark (carried forward from earlier session) — if under ~$15 CPL, pause the old SAC campaign `120241691882900665`. (7) (Carried forward) Send the 48 SendGrid backfill emails. (8) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist. (9) (Carried forward) Rotate Zoom + Facebook secrets sent in plaintext earlier.

### 2026-05-11 (PM) — Federal Workshops ad rebuild: killed SAC cap on targeting, replaced with educational LAL clone of the Webinar pattern

- **Root cause of "the Workshops ads suck":** Campaign `120241691882900665` ("CW — Federal Workshops — May 19-21 — Leads") was flagged with `special_ad_categories = ['FINANCIAL_PRODUCTS_SERVICES']`, which strips detailed targeting / employer targeting / sub-1% LAL / narrow age windows. Running at **$29.18 CPL** for 30d. Compare: the Webinar campaign (`120241615030540665`) with `special_ad_categories = []` is running at **$10.80 CPL** off the same Fed Employees Source List using a 1% LAL — 2.7x cheaper leads. The old "Federal Seminar Event" the user remembered as "successful" was actually the worst performer in account history at $166.28 CPL.
- **What shipped (live on FB account `act_956377226671159` as of this session):** New campaign `120241962930110665` "CW — Federal Workshops — Educational (May 19-21)", no SAC, OUTCOME_LEADS, OFFSITE_CONVERSIONS optimization, pixel LEAD event. Three ad sets, each $350/day, LAL 1% of `Fed Employees - Source List` (audience id `120241513320320665`) with `Fed Employees - Source List` + CRM List excluded, age 25-65, Advantage+ placements: AS1 Ogden (id `120241962936030665`, city key 2532136 ±25mi, ends Tue May 19 1pm), AS2 SLC (id `120241963276230665`, city key 2532348 ±19mi, ends Wed May 20 1pm), AS3 Hill AFB/Layton (id `120241963280110665`, city key 2531858 ±15mi, ends Thu May 21 3pm). Nine ads total (3 angles × 3 locations): Authority/Five-Systems/Decisions. Reused proven Webinar image hash `e09210ed50e9005befa6f405790303b4`. UTMs: `utm_campaign=fed-workshops-edu-may2026&utm_content=<slug>-<angle>`. Old SAC campaign still ACTIVE intentionally — see pickup #1 below.
- **Copy softening per non-promissory RIA language standard ([PR #23](https://github.com/Gull-Stack/capital-wealth/pull/23), merged):** H1 on all 3 workshop landing pages changed from "Two hours that **change** thirty years…" → "Two hours that **could shape** thirty years…". All 9 ad bodies use "could shape", "can shape", "may", never "will". Same PR added `/privacy/` ([src/privacy/index.njk](../src/privacy/index.njk)) — was 404 site-wide despite being linked from calculator, SDBA, and other pages' footers. Created to satisfy Meta App publish requirement AND fix the broken site-wide link.
- **FB App + System User permission gotchas hit (and resolved) on the way:** (a) "Capital Wealth Ads" FB App (id `831374225889248`) was in Development mode — couldn't attach API-built creatives to ads, error "Ads creative post was created by an app that is in development mode." Fix: user flipped App Mode to Live in `developers.facebook.com/apps/831374225889248/settings/basic/` after filling Privacy Policy URL (the now-live `/privacy/`), App Icon, Category, Contact email. (b) System User "Melvin" (id `122111746130947675`, the owner of `FB_ACCESS_TOKEN`) had zero pages assigned — error "Missing Page permission… contact admin for Advertiser role or higher." Fix: user went to `business.facebook.com/settings/system-users` → Melvin → Add Assets → Pages → Capital Wealth (id `1472234546327675`) → toggled on "Manage Page" → Save. (c) Deprecated `degrees_of_freedom_spec.creative_features_spec.standard_enhancements.enroll_status` field — removed from creative spec. (d) `is_adset_budget_sharing_enabled=false` is now required at campaign creation when not using campaign budget.
- **Tooling (in [PR #25](https://github.com/Gull-Stack/capital-wealth/pull/25)):** Six zero-dependency Node scripts under `scripts/fb-*.js` with inline `.env` loader. `fb-diagnose.js` (multi-window insights + auto findings — dead ads, fatigue, low CTR, CPL trend decay, demographic leaks). `fb-compare-events.js` (lifetime Seminar vs Workshop vs Webinar with SAC status). `fb-build-workshops-edu.js` (idempotent builder — reuses campaign/adsets/ads by name on re-run; supports `--dry-run` and `--campaign-id <id>`). `fb-launch.js` (pre-flight ad review check then flips ACTIVE). `fb-pull-rebuild-inputs.js` / `fb-pull-city-keys.js` (read-only discovery). `tmp/` gitignored.
- **What to pick up next:** (1) **~48 hours from now** (around 2026-05-13 PM), check the new campaign's CPL — if it's tracking under ~$15 (i.e., Webinar-like rather than SAC-like), **pause** the old SAC campaign `120241691882900665` to redirect spend. Easiest: `node scripts/fb-diagnose.js`, then a single API call to set `status=PAUSED`. (2) **Review the privacy policy text** at [src/privacy/index.njk](../src/privacy/index.njk) for accuracy with actual data practices — I drafted it conservatively but a real compliance review is warranted. (3) (Carried forward) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py`). (4) (Carried forward) Rotate Zoom + Facebook secrets that were sent in plaintext during earlier sessions. (5) (Carried forward) Have Josh add `Billboard` to SF Lead.LeadSource picklist.

### 2026-05-11 — utahfed.com / idahofed.com billboard hub → federal-workshops chooser page

- **What shipped (this PR on branch `claude/flamboyant-ishizaka-55885b`):** New hub landing page at [src/l/federal-workshops/index.njk](../src/l/federal-workshops/index.njk) that lists the three May 2026 federal workshops as clickable cards (Ogden 5/19, SLC 5/20, Hill AFB 5/21). Each card CTA points to its existing per-workshop landing page with `?lead_source=Billboard` appended. The three existing workshop pages ([src/l/federal-benefits-workshop-{ogden,slc,hill-afb}/index.njk](../src/l/)) gained a `<input type="hidden" name="lead_source" id="lead_source">` near line 944 and the inline submit handler's query-param copier now includes `'lead_source'` alongside the UTM keys, so the value flows through to `/api/submit-lead`. [api/submit-lead.js:146](../api/submit-lead.js:146) `ALLOWED_LEAD_SOURCES` set now includes `'Billboard'`. [vercel.json:24](../vercel.json) gained four host-based redirects (utahfed.com, www.utahfed.com, idahofed.com, www.idahofed.com → `https://www.capitalwealth.com/l/federal-workshops/?lead_source=Billboard`) modeled on the existing `retireutah.com` pattern.
- **Why the redirect carries `?lead_source=Billboard` itself:** Defense-in-depth. If a billboard visitor lands on the hub and shares the link with a friend, that friend's session may not have the query string when they click a workshop card — but the hub→workshop link bakes `?lead_source=Billboard` into the href, so attribution still survives the second hop.
- **Salesforce picklist note:** `Lead.LeadSource` is **not a restricted picklist** (`restrictedPicklist: false` per the SObject describe), so SF will happily accept `"Billboard"` even though it's not currently in the picklist values — leads will save fine the moment code deploys. **However**, until Josh adds `Billboard` to the picklist via SF Setup → Lead → Fields → LeadSource, it won't appear as a filter option in reports/dashboards. This is a 30-second admin task; not a blocker for shipping.
- **DNS / Vercel domains dependency (NOT a code task):** The four redirect rules only fire once `utahfed.com`, `www.utahfed.com`, `idahofed.com`, `www.idahofed.com` are attached to the Vercel project (Settings → Domains) AND DNS at the registrar points to Vercel (typically A record `76.76.21.21` for apex, CNAME `cname.vercel-dns.com` for www). Confirm with whoever holds the registrar logins for these two domains before announcing the billboards. Until then, the hub page at `/l/federal-workshops/` still works on capitalwealth.com directly.
- **Verification after merge → main → Vercel deploy:** (1) Visit `https://www.capitalwealth.com/l/federal-workshops/` — three cards render in date order, navy/gold + federal palette, compliance footer present. (2) Click any card → URL on destination should contain `?lead_source=Billboard`; complete the form with a test address like `brycedmorgan+billboard-test@gmail.com`; then `sf data query -o cw -q "SELECT Id, Email, LeadSource FROM Lead WHERE Email='brycedmorgan+billboard-test@gmail.com' ORDER BY CreatedDate DESC LIMIT 1"` — expect `LeadSource = 'Billboard'`. (3) Negative case: submit a fresh lead from a workshop page **without** the query string — `LeadSource` should fall back to `'Website Form'`. (4) Once DNS is live: `curl -sI https://utahfed.com/` and `curl -sI https://idahofed.com/` should both 308 to the hub URL with the query string.
- **What to pick up next:** (1) Open the PR for this branch and ship. (2) Confirm the DNS / Vercel domain attachment for both apex domains. (3) Have Josh add `Billboard` to the SF Lead.LeadSource picklist so it shows up cleanly in report filters. (4) (Carried forward) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py`). (5) (Carried forward) Rotate Zoom + Facebook secrets that were sent in plaintext during the previous session.

---

### 2026-05-11 — Federal Webinar registration pipeline: env fix, SF patch, 48-lead backfill, broken phone validation hotfix

- **Root cause of "Zoom Events count looks wrong" concern:** The Zoom Events count (161/165) was actually correct — the Webinar+ migration ported registrations cleanly. The real issue was that the website form's Zoom integration ([api/submit-lead.js](../api/submit-lead.js)) had been silently no-op'ing since it shipped May 5: all four `ZOOM_*` env vars in Vercel production were empty strings. Every website-form submission since May 5 created an SF Lead but no Zoom registrant.
- **Env vars set in Vercel production:** `ZOOM_ACCOUNT_ID`, `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, `ZOOM_WEBINAR_ID=82502463485` (underlying webinar inside the migrated Zoom Events session — same numeric ID still works with legacy `/v2/webinars/{id}/registrants` API). Plus `FB_ACCESS_TOKEN` + `FB_AD_ACCOUNT_ID=956377226671159` for Facebook Ads spend pulls via [api/facebook-ads.js](../api/facebook-ads.js). Preview left empty intentionally.
- **Two real bugs patched in [PR #21](https://github.com/Gull-Stack/capital-wealth/pull/21):** (1) SF Lead update path was 400-ing because a validation rule now requires `How_did_you_hear_about_us__c` on update — patched to backfill `'Website'` when blank. (2) TY page `.ty-action-btn` CSS overrode the `[hidden]` HTML attribute, so the "Save Zoom Join Link" button rendered with `href="#"` even when no join URL was available — patched with `[hidden] !important` reset.
- **Urgent hotfix [PR #22](https://github.com/Gull-Stack/capital-wealth/pull/22) — phone validation broken site-wide:** [PR #20](https://github.com/Gull-Stack/capital-wealth/pull/20) moved phone validation into the shared `window.CWPhone` helper in [src/assets/js/main.js](../src/assets/js/main.js) but `base-finpay.njk` (the federal landing page layout) never loaded `main.js`. Result: `window.CWPhone` was `undefined`, every form submission rejected with "Please enter a valid US phone number." Fix: add `<script src="/assets/js/main.js">` before `</body>` in [src/_includes/layouts/base-finpay.njk](../src/_includes/layouts/base-finpay.njk).
- **Backfill completed:** 48 affected website-form leads (May 5–11 window) registered with Zoom via single-registrant API. Each SF Lead Description appended with `Zoom Registrant ID` + `Zoom Join URL`. Confirmation emails suppressed via webinar setting (`registrants_confirmation_email: false`) — Zoom Events' built-in reminder system will deliver join links pre-event. SendGrid follow-up email TO THE 48 still pending user OK.
- **All 253 webinar leads (LeadSource='Webinar' since May 5) marked `Are_You_Federal__c = 'Yes'`** via SF bulk update. Per Josh: "These are all federal leads."
- **SF Campaign work:** Added 203 missing Leads as CampaignMembers to `701VS00000eWrYVYA0` (Federal Benefits Workshop 5.14.26) — Campaign now has all 253. Created Salesforce Report **`Federal Webinar 5.14.26 — Members & $`** (Id `00OVS000008BqDB2A0`) showing registrants + Actual Cost. Set `Campaign.ActualCost = $5,153.37` from FB campaign `CW — Federal Webinar — May 14 — Leads` (id `120241615030540665`).
- **What to pick up next:** (1) Send the 48 SendGrid backfill emails (script staged at `/tmp/send_backfill.py` — needs user "send" confirmation). (2) Delete 3 synthetic Zoom registrants left from diagnosis: `claude-diag-probe-*@example.com`, `claude-probe2-*@example.com`, `brycedmorgan+webprobe1778514614@gmail.com` — easiest in the Zoom Events admin UI under registrant list. (3) **Rotate the Zoom Server-to-Server OAuth client secret** since it was sent in plaintext chat. (4) Rotate the Facebook system-user access token (also sent in plaintext). (5) Optionally wire a Vercel cron to refresh Campaign.ActualCost daily from Facebook spend.
- **Reference:** Zoom webinar host email is `bmorgan@capitalwealth.com` — Bryce can't self-test the registration form with his own email (Zoom rejects host as registrant). Webinar's `registrants_confirmation_email` is OFF — Zoom doesn't email registrants automatically; rely on Zoom Events reminders or our own SendGrid.
